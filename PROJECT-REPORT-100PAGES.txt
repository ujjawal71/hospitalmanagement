ONLINE DOCTOR APPOINTMENT BOOKING SYSTEM
========================================

FULL PROJECT REPORT (EXPANDED) — TEXT FOR PDF / WORD
----------------------------------------------------

Generated for: Hospital Management / Doctor Appointment project
Stack: Java 17, Spring Boot 3.2, Spring Data JPA, Spring Security, MySQL
Frontend: HTML, CSS, JavaScript (static under src/main/resources/static)
Default Admin: admin@hospital.com / admin123
Default Doctor: doctor@hospital.com / doctor123

NOTE: Page count depends on font, margins, and line spacing in Word/PDF.
This file is long (~100 A4 pages typical at 11pt, 1.15 line spacing, ~45 lines/page).

DECLARATION
===========

I declare that this report titled Online Doctor Appointment Booking System is based on my
own work, project codebase study, and standard software engineering references. Information
from public documentation is cited in the bibliography. This document is prepared for
academic and project submission purposes.

ACKNOWLEDGEMENT
===============

I express sincere thanks to guides, faculty, peers, and open-source communities whose
materials helped understand Spring Boot, REST APIs, web security, and relational database
design. Special thanks to everyone who tested the prototype and gave feedback on usability.

ABSTRACT
========

The Online Doctor Appointment Booking System is a full-stack web-oriented application that
integrates patient self-service, doctor workflow tools, and administrative control in one
place. Patients register, authenticate, discover doctors by department, and book time-bound
appointments. Doctors manage schedules through time slots and process appointment queues
with status updates. Administrators curate departments and doctor master data and monitor
system-wide appointments and users. The backend exposes REST endpoints secured with
token-based access. The relational schema models entities with integrity constraints.
Entity–Relationship diagrams document structure; Data Flow Diagrams document how information
moves among actors and data stores. The report also covers requirements, design,
implementation outline, testing, deployment notes, and future enhancements.

TABLE OF CONTENTS (STRUCTURE)
=============================

1. Introduction
2 Problem Definition & Objectives
3 Literature Review & Related Work
4 Planning & Feasibility Study (Technical, Economic, Operational)
5 Software Requirements Specification (SRS)
6 Analysis — Actors, Use Cases, Activity Outline
7 System Design — Architecture, Modules, Layering
8 Data Design — ER Diagram, Normalization Notes, Schema Mapping
9 Data Flow Design — DFD Level 0, Level 1, Data Dictionary
10 Detailed Design — API Contracts, DTOs, Security Model
11 Implementation Notes — Controllers, Services, Repositories, Frontend
12 Database Tables & Field Specifications
13 User Interface — Screens & Navigation (Narrative)
14 Testing Strategy — Unit, Integration, UAT Matrices
15 Deployment & Operations — Environment, Run Instructions, Risks
16 Security & Privacy Considerations
17 Limitations, Future Scope, Conclusion
18 Bibliography & References
19 Appendices — Mermaid Source, Sample Test Data, Checklists

1. INTRODUCTION
===============

1.1 Background segment 1. Healthcare delivery increasingly depends on digital channels for
patient intake and scheduling. Manual booking via phone creates bottlenecks, increases
errors, and limits visibility for both staff and patients. A centralized online appointment
system reduces queue time, improves record consistency, and supports administrators in
coordinating doctors across departments. This project implements a representative solution
using industry-common technologies (Java/Spring Boot, MySQL) so that the design trade-offs
are clear, portable, and extensible toward production hardening. Segment 1 reinforces how
each role—patient, doctor, admin—interacts with the same core data model while following
least-privilege access through role-scoped APIs and token validation filters.

1.2 Background segment 2. Healthcare delivery increasingly depends on digital channels for
patient intake and scheduling. Manual booking via phone creates bottlenecks, increases
errors, and limits visibility for both staff and patients. A centralized online appointment
system reduces queue time, improves record consistency, and supports administrators in
coordinating doctors across departments. This project implements a representative solution
using industry-common technologies (Java/Spring Boot, MySQL) so that the design trade-offs
are clear, portable, and extensible toward production hardening. Segment 2 reinforces how
each role—patient, doctor, admin—interacts with the same core data model while following
least-privilege access through role-scoped APIs and token validation filters.

1.3 Background segment 3. Healthcare delivery increasingly depends on digital channels for
patient intake and scheduling. Manual booking via phone creates bottlenecks, increases
errors, and limits visibility for both staff and patients. A centralized online appointment
system reduces queue time, improves record consistency, and supports administrators in
coordinating doctors across departments. This project implements a representative solution
using industry-common technologies (Java/Spring Boot, MySQL) so that the design trade-offs
are clear, portable, and extensible toward production hardening. Segment 3 reinforces how
each role—patient, doctor, admin—interacts with the same core data model while following
least-privilege access through role-scoped APIs and token validation filters.

1.4 Background segment 4. Healthcare delivery increasingly depends on digital channels for
patient intake and scheduling. Manual booking via phone creates bottlenecks, increases
errors, and limits visibility for both staff and patients. A centralized online appointment
system reduces queue time, improves record consistency, and supports administrators in
coordinating doctors across departments. This project implements a representative solution
using industry-common technologies (Java/Spring Boot, MySQL) so that the design trade-offs
are clear, portable, and extensible toward production hardening. Segment 4 reinforces how
each role—patient, doctor, admin—interacts with the same core data model while following
least-privilege access through role-scoped APIs and token validation filters.

1.5 Background segment 5. Healthcare delivery increasingly depends on digital channels for
patient intake and scheduling. Manual booking via phone creates bottlenecks, increases
errors, and limits visibility for both staff and patients. A centralized online appointment
system reduces queue time, improves record consistency, and supports administrators in
coordinating doctors across departments. This project implements a representative solution
using industry-common technologies (Java/Spring Boot, MySQL) so that the design trade-offs
are clear, portable, and extensible toward production hardening. Segment 5 reinforces how
each role—patient, doctor, admin—interacts with the same core data model while following
least-privilege access through role-scoped APIs and token validation filters.

1.6 Background segment 6. Healthcare delivery increasingly depends on digital channels for
patient intake and scheduling. Manual booking via phone creates bottlenecks, increases
errors, and limits visibility for both staff and patients. A centralized online appointment
system reduces queue time, improves record consistency, and supports administrators in
coordinating doctors across departments. This project implements a representative solution
using industry-common technologies (Java/Spring Boot, MySQL) so that the design trade-offs
are clear, portable, and extensible toward production hardening. Segment 6 reinforces how
each role—patient, doctor, admin—interacts with the same core data model while following
least-privilege access through role-scoped APIs and token validation filters.

1.7 Background segment 7. Healthcare delivery increasingly depends on digital channels for
patient intake and scheduling. Manual booking via phone creates bottlenecks, increases
errors, and limits visibility for both staff and patients. A centralized online appointment
system reduces queue time, improves record consistency, and supports administrators in
coordinating doctors across departments. This project implements a representative solution
using industry-common technologies (Java/Spring Boot, MySQL) so that the design trade-offs
are clear, portable, and extensible toward production hardening. Segment 7 reinforces how
each role—patient, doctor, admin—interacts with the same core data model while following
least-privilege access through role-scoped APIs and token validation filters.

1.8 Background segment 8. Healthcare delivery increasingly depends on digital channels for
patient intake and scheduling. Manual booking via phone creates bottlenecks, increases
errors, and limits visibility for both staff and patients. A centralized online appointment
system reduces queue time, improves record consistency, and supports administrators in
coordinating doctors across departments. This project implements a representative solution
using industry-common technologies (Java/Spring Boot, MySQL) so that the design trade-offs
are clear, portable, and extensible toward production hardening. Segment 8 reinforces how
each role—patient, doctor, admin—interacts with the same core data model while following
least-privilege access through role-scoped APIs and token validation filters.

1.9 Background segment 9. Healthcare delivery increasingly depends on digital channels for
patient intake and scheduling. Manual booking via phone creates bottlenecks, increases
errors, and limits visibility for both staff and patients. A centralized online appointment
system reduces queue time, improves record consistency, and supports administrators in
coordinating doctors across departments. This project implements a representative solution
using industry-common technologies (Java/Spring Boot, MySQL) so that the design trade-offs
are clear, portable, and extensible toward production hardening. Segment 9 reinforces how
each role—patient, doctor, admin—interacts with the same core data model while following
least-privilege access through role-scoped APIs and token validation filters.

1.10 Background segment 10. Healthcare delivery increasingly depends on digital channels for
patient intake and scheduling. Manual booking via phone creates bottlenecks, increases
errors, and limits visibility for both staff and patients. A centralized online appointment
system reduces queue time, improves record consistency, and supports administrators in
coordinating doctors across departments. This project implements a representative solution
using industry-common technologies (Java/Spring Boot, MySQL) so that the design trade-offs
are clear, portable, and extensible toward production hardening. Segment 10 reinforces how
each role—patient, doctor, admin—interacts with the same core data model while following
least-privilege access through role-scoped APIs and token validation filters.

1.11 Background segment 11. Healthcare delivery increasingly depends on digital channels for
patient intake and scheduling. Manual booking via phone creates bottlenecks, increases
errors, and limits visibility for both staff and patients. A centralized online appointment
system reduces queue time, improves record consistency, and supports administrators in
coordinating doctors across departments. This project implements a representative solution
using industry-common technologies (Java/Spring Boot, MySQL) so that the design trade-offs
are clear, portable, and extensible toward production hardening. Segment 11 reinforces how
each role—patient, doctor, admin—interacts with the same core data model while following
least-privilege access through role-scoped APIs and token validation filters.

1.12 Background segment 12. Healthcare delivery increasingly depends on digital channels for
patient intake and scheduling. Manual booking via phone creates bottlenecks, increases
errors, and limits visibility for both staff and patients. A centralized online appointment
system reduces queue time, improves record consistency, and supports administrators in
coordinating doctors across departments. This project implements a representative solution
using industry-common technologies (Java/Spring Boot, MySQL) so that the design trade-offs
are clear, portable, and extensible toward production hardening. Segment 12 reinforces how
each role—patient, doctor, admin—interacts with the same core data model while following
least-privilege access through role-scoped APIs and token validation filters.

1.13 Background segment 13. Healthcare delivery increasingly depends on digital channels for
patient intake and scheduling. Manual booking via phone creates bottlenecks, increases
errors, and limits visibility for both staff and patients. A centralized online appointment
system reduces queue time, improves record consistency, and supports administrators in
coordinating doctors across departments. This project implements a representative solution
using industry-common technologies (Java/Spring Boot, MySQL) so that the design trade-offs
are clear, portable, and extensible toward production hardening. Segment 13 reinforces how
each role—patient, doctor, admin—interacts with the same core data model while following
least-privilege access through role-scoped APIs and token validation filters.

1.14 Background segment 14. Healthcare delivery increasingly depends on digital channels for
patient intake and scheduling. Manual booking via phone creates bottlenecks, increases
errors, and limits visibility for both staff and patients. A centralized online appointment
system reduces queue time, improves record consistency, and supports administrators in
coordinating doctors across departments. This project implements a representative solution
using industry-common technologies (Java/Spring Boot, MySQL) so that the design trade-offs
are clear, portable, and extensible toward production hardening. Segment 14 reinforces how
each role—patient, doctor, admin—interacts with the same core data model while following
least-privilege access through role-scoped APIs and token validation filters.

1.15 Background segment 15. Healthcare delivery increasingly depends on digital channels for
patient intake and scheduling. Manual booking via phone creates bottlenecks, increases
errors, and limits visibility for both staff and patients. A centralized online appointment
system reduces queue time, improves record consistency, and supports administrators in
coordinating doctors across departments. This project implements a representative solution
using industry-common technologies (Java/Spring Boot, MySQL) so that the design trade-offs
are clear, portable, and extensible toward production hardening. Segment 15 reinforces how
each role—patient, doctor, admin—interacts with the same core data model while following
least-privilege access through role-scoped APIs and token validation filters.

1.16 Background segment 16. Healthcare delivery increasingly depends on digital channels for
patient intake and scheduling. Manual booking via phone creates bottlenecks, increases
errors, and limits visibility for both staff and patients. A centralized online appointment
system reduces queue time, improves record consistency, and supports administrators in
coordinating doctors across departments. This project implements a representative solution
using industry-common technologies (Java/Spring Boot, MySQL) so that the design trade-offs
are clear, portable, and extensible toward production hardening. Segment 16 reinforces how
each role—patient, doctor, admin—interacts with the same core data model while following
least-privilege access through role-scoped APIs and token validation filters.

1.17 Background segment 17. Healthcare delivery increasingly depends on digital channels for
patient intake and scheduling. Manual booking via phone creates bottlenecks, increases
errors, and limits visibility for both staff and patients. A centralized online appointment
system reduces queue time, improves record consistency, and supports administrators in
coordinating doctors across departments. This project implements a representative solution
using industry-common technologies (Java/Spring Boot, MySQL) so that the design trade-offs
are clear, portable, and extensible toward production hardening. Segment 17 reinforces how
each role—patient, doctor, admin—interacts with the same core data model while following
least-privilege access through role-scoped APIs and token validation filters.

1.18 Background segment 18. Healthcare delivery increasingly depends on digital channels for
patient intake and scheduling. Manual booking via phone creates bottlenecks, increases
errors, and limits visibility for both staff and patients. A centralized online appointment
system reduces queue time, improves record consistency, and supports administrators in
coordinating doctors across departments. This project implements a representative solution
using industry-common technologies (Java/Spring Boot, MySQL) so that the design trade-offs
are clear, portable, and extensible toward production hardening. Segment 18 reinforces how
each role—patient, doctor, admin—interacts with the same core data model while following
least-privilege access through role-scoped APIs and token validation filters.

1.19 Background segment 19. Healthcare delivery increasingly depends on digital channels for
patient intake and scheduling. Manual booking via phone creates bottlenecks, increases
errors, and limits visibility for both staff and patients. A centralized online appointment
system reduces queue time, improves record consistency, and supports administrators in
coordinating doctors across departments. This project implements a representative solution
using industry-common technologies (Java/Spring Boot, MySQL) so that the design trade-offs
are clear, portable, and extensible toward production hardening. Segment 19 reinforces how
each role—patient, doctor, admin—interacts with the same core data model while following
least-privilege access through role-scoped APIs and token validation filters.

1.20 Background segment 20. Healthcare delivery increasingly depends on digital channels for
patient intake and scheduling. Manual booking via phone creates bottlenecks, increases
errors, and limits visibility for both staff and patients. A centralized online appointment
system reduces queue time, improves record consistency, and supports administrators in
coordinating doctors across departments. This project implements a representative solution
using industry-common technologies (Java/Spring Boot, MySQL) so that the design trade-offs
are clear, portable, and extensible toward production hardening. Segment 20 reinforces how
each role—patient, doctor, admin—interacts with the same core data model while following
least-privilege access through role-scoped APIs and token validation filters.

1.21 Background segment 21. Healthcare delivery increasingly depends on digital channels for
patient intake and scheduling. Manual booking via phone creates bottlenecks, increases
errors, and limits visibility for both staff and patients. A centralized online appointment
system reduces queue time, improves record consistency, and supports administrators in
coordinating doctors across departments. This project implements a representative solution
using industry-common technologies (Java/Spring Boot, MySQL) so that the design trade-offs
are clear, portable, and extensible toward production hardening. Segment 21 reinforces how
each role—patient, doctor, admin—interacts with the same core data model while following
least-privilege access through role-scoped APIs and token validation filters.

1.22 Background segment 22. Healthcare delivery increasingly depends on digital channels for
patient intake and scheduling. Manual booking via phone creates bottlenecks, increases
errors, and limits visibility for both staff and patients. A centralized online appointment
system reduces queue time, improves record consistency, and supports administrators in
coordinating doctors across departments. This project implements a representative solution
using industry-common technologies (Java/Spring Boot, MySQL) so that the design trade-offs
are clear, portable, and extensible toward production hardening. Segment 22 reinforces how
each role—patient, doctor, admin—interacts with the same core data model while following
least-privilege access through role-scoped APIs and token validation filters.

1.23 Background segment 23. Healthcare delivery increasingly depends on digital channels for
patient intake and scheduling. Manual booking via phone creates bottlenecks, increases
errors, and limits visibility for both staff and patients. A centralized online appointment
system reduces queue time, improves record consistency, and supports administrators in
coordinating doctors across departments. This project implements a representative solution
using industry-common technologies (Java/Spring Boot, MySQL) so that the design trade-offs
are clear, portable, and extensible toward production hardening. Segment 23 reinforces how
each role—patient, doctor, admin—interacts with the same core data model while following
least-privilege access through role-scoped APIs and token validation filters.

1.24 Background segment 24. Healthcare delivery increasingly depends on digital channels for
patient intake and scheduling. Manual booking via phone creates bottlenecks, increases
errors, and limits visibility for both staff and patients. A centralized online appointment
system reduces queue time, improves record consistency, and supports administrators in
coordinating doctors across departments. This project implements a representative solution
using industry-common technologies (Java/Spring Boot, MySQL) so that the design trade-offs
are clear, portable, and extensible toward production hardening. Segment 24 reinforces how
each role—patient, doctor, admin—interacts with the same core data model while following
least-privilege access through role-scoped APIs and token validation filters.

1.25 Background segment 25. Healthcare delivery increasingly depends on digital channels for
patient intake and scheduling. Manual booking via phone creates bottlenecks, increases
errors, and limits visibility for both staff and patients. A centralized online appointment
system reduces queue time, improves record consistency, and supports administrators in
coordinating doctors across departments. This project implements a representative solution
using industry-common technologies (Java/Spring Boot, MySQL) so that the design trade-offs
are clear, portable, and extensible toward production hardening. Segment 25 reinforces how
each role—patient, doctor, admin—interacts with the same core data model while following
least-privilege access through role-scoped APIs and token validation filters.

1.26 Background segment 26. Healthcare delivery increasingly depends on digital channels for
patient intake and scheduling. Manual booking via phone creates bottlenecks, increases
errors, and limits visibility for both staff and patients. A centralized online appointment
system reduces queue time, improves record consistency, and supports administrators in
coordinating doctors across departments. This project implements a representative solution
using industry-common technologies (Java/Spring Boot, MySQL) so that the design trade-offs
are clear, portable, and extensible toward production hardening. Segment 26 reinforces how
each role—patient, doctor, admin—interacts with the same core data model while following
least-privilege access through role-scoped APIs and token validation filters.

1.27 Background segment 27. Healthcare delivery increasingly depends on digital channels for
patient intake and scheduling. Manual booking via phone creates bottlenecks, increases
errors, and limits visibility for both staff and patients. A centralized online appointment
system reduces queue time, improves record consistency, and supports administrators in
coordinating doctors across departments. This project implements a representative solution
using industry-common technologies (Java/Spring Boot, MySQL) so that the design trade-offs
are clear, portable, and extensible toward production hardening. Segment 27 reinforces how
each role—patient, doctor, admin—interacts with the same core data model while following
least-privilege access through role-scoped APIs and token validation filters.

2. PROBLEM DEFINITION & OBJECTIVES
==================================

OBJ-1: Enable secure multi-role authentication (Admin, Doctor, Patient).
OBJ-2: Let patients search doctors by department and book appointments with date/time.
OBJ-3: Let doctors view their appointments, update status, view patient details, maintain time slots.
OBJ-4: Let admins CRUD departments and doctors and view all appointments and manage users.
OBJ-5: Provide public read endpoints for landing pages (department list, doctors by department).
OBJ-6: Persist data in MySQL with referential integrity and JPA mapping.
OBJ-7: Serve a lightweight HTML/CSS/JS UI from the same Spring Boot artifact.

2.1 Problem elaboration 1. The problem domain combines scheduling constraints (doctor
availability, slot conflicts) with identity management (unique emails, passwords) and
authorization (each role must not access others' administrative functions). The solution
must remain simple enough for a college project while demonstrating layered architecture,
RESTful design, validation, and clear separation between web static assets and API
endpoints. Block 1 documents rationale for iterative refinement: first stabilize auth and
master data, then appointments, then doctor time slots, then polish UI flows.

2.2 Problem elaboration 2. The problem domain combines scheduling constraints (doctor
availability, slot conflicts) with identity management (unique emails, passwords) and
authorization (each role must not access others' administrative functions). The solution
must remain simple enough for a college project while demonstrating layered architecture,
RESTful design, validation, and clear separation between web static assets and API
endpoints. Block 2 documents rationale for iterative refinement: first stabilize auth and
master data, then appointments, then doctor time slots, then polish UI flows.

2.3 Problem elaboration 3. The problem domain combines scheduling constraints (doctor
availability, slot conflicts) with identity management (unique emails, passwords) and
authorization (each role must not access others' administrative functions). The solution
must remain simple enough for a college project while demonstrating layered architecture,
RESTful design, validation, and clear separation between web static assets and API
endpoints. Block 3 documents rationale for iterative refinement: first stabilize auth and
master data, then appointments, then doctor time slots, then polish UI flows.

2.4 Problem elaboration 4. The problem domain combines scheduling constraints (doctor
availability, slot conflicts) with identity management (unique emails, passwords) and
authorization (each role must not access others' administrative functions). The solution
must remain simple enough for a college project while demonstrating layered architecture,
RESTful design, validation, and clear separation between web static assets and API
endpoints. Block 4 documents rationale for iterative refinement: first stabilize auth and
master data, then appointments, then doctor time slots, then polish UI flows.

2.5 Problem elaboration 5. The problem domain combines scheduling constraints (doctor
availability, slot conflicts) with identity management (unique emails, passwords) and
authorization (each role must not access others' administrative functions). The solution
must remain simple enough for a college project while demonstrating layered architecture,
RESTful design, validation, and clear separation between web static assets and API
endpoints. Block 5 documents rationale for iterative refinement: first stabilize auth and
master data, then appointments, then doctor time slots, then polish UI flows.

2.6 Problem elaboration 6. The problem domain combines scheduling constraints (doctor
availability, slot conflicts) with identity management (unique emails, passwords) and
authorization (each role must not access others' administrative functions). The solution
must remain simple enough for a college project while demonstrating layered architecture,
RESTful design, validation, and clear separation between web static assets and API
endpoints. Block 6 documents rationale for iterative refinement: first stabilize auth and
master data, then appointments, then doctor time slots, then polish UI flows.

2.7 Problem elaboration 7. The problem domain combines scheduling constraints (doctor
availability, slot conflicts) with identity management (unique emails, passwords) and
authorization (each role must not access others' administrative functions). The solution
must remain simple enough for a college project while demonstrating layered architecture,
RESTful design, validation, and clear separation between web static assets and API
endpoints. Block 7 documents rationale for iterative refinement: first stabilize auth and
master data, then appointments, then doctor time slots, then polish UI flows.

2.8 Problem elaboration 8. The problem domain combines scheduling constraints (doctor
availability, slot conflicts) with identity management (unique emails, passwords) and
authorization (each role must not access others' administrative functions). The solution
must remain simple enough for a college project while demonstrating layered architecture,
RESTful design, validation, and clear separation between web static assets and API
endpoints. Block 8 documents rationale for iterative refinement: first stabilize auth and
master data, then appointments, then doctor time slots, then polish UI flows.

2.9 Problem elaboration 9. The problem domain combines scheduling constraints (doctor
availability, slot conflicts) with identity management (unique emails, passwords) and
authorization (each role must not access others' administrative functions). The solution
must remain simple enough for a college project while demonstrating layered architecture,
RESTful design, validation, and clear separation between web static assets and API
endpoints. Block 9 documents rationale for iterative refinement: first stabilize auth and
master data, then appointments, then doctor time slots, then polish UI flows.

2.10 Problem elaboration 10. The problem domain combines scheduling constraints (doctor
availability, slot conflicts) with identity management (unique emails, passwords) and
authorization (each role must not access others' administrative functions). The solution
must remain simple enough for a college project while demonstrating layered architecture,
RESTful design, validation, and clear separation between web static assets and API
endpoints. Block 10 documents rationale for iterative refinement: first stabilize auth and
master data, then appointments, then doctor time slots, then polish UI flows.

2.11 Problem elaboration 11. The problem domain combines scheduling constraints (doctor
availability, slot conflicts) with identity management (unique emails, passwords) and
authorization (each role must not access others' administrative functions). The solution
must remain simple enough for a college project while demonstrating layered architecture,
RESTful design, validation, and clear separation between web static assets and API
endpoints. Block 11 documents rationale for iterative refinement: first stabilize auth and
master data, then appointments, then doctor time slots, then polish UI flows.

2.12 Problem elaboration 12. The problem domain combines scheduling constraints (doctor
availability, slot conflicts) with identity management (unique emails, passwords) and
authorization (each role must not access others' administrative functions). The solution
must remain simple enough for a college project while demonstrating layered architecture,
RESTful design, validation, and clear separation between web static assets and API
endpoints. Block 12 documents rationale for iterative refinement: first stabilize auth and
master data, then appointments, then doctor time slots, then polish UI flows.

2.13 Problem elaboration 13. The problem domain combines scheduling constraints (doctor
availability, slot conflicts) with identity management (unique emails, passwords) and
authorization (each role must not access others' administrative functions). The solution
must remain simple enough for a college project while demonstrating layered architecture,
RESTful design, validation, and clear separation between web static assets and API
endpoints. Block 13 documents rationale for iterative refinement: first stabilize auth and
master data, then appointments, then doctor time slots, then polish UI flows.

2.14 Problem elaboration 14. The problem domain combines scheduling constraints (doctor
availability, slot conflicts) with identity management (unique emails, passwords) and
authorization (each role must not access others' administrative functions). The solution
must remain simple enough for a college project while demonstrating layered architecture,
RESTful design, validation, and clear separation between web static assets and API
endpoints. Block 14 documents rationale for iterative refinement: first stabilize auth and
master data, then appointments, then doctor time slots, then polish UI flows.

2.15 Problem elaboration 15. The problem domain combines scheduling constraints (doctor
availability, slot conflicts) with identity management (unique emails, passwords) and
authorization (each role must not access others' administrative functions). The solution
must remain simple enough for a college project while demonstrating layered architecture,
RESTful design, validation, and clear separation between web static assets and API
endpoints. Block 15 documents rationale for iterative refinement: first stabilize auth and
master data, then appointments, then doctor time slots, then polish UI flows.

2.16 Problem elaboration 16. The problem domain combines scheduling constraints (doctor
availability, slot conflicts) with identity management (unique emails, passwords) and
authorization (each role must not access others' administrative functions). The solution
must remain simple enough for a college project while demonstrating layered architecture,
RESTful design, validation, and clear separation between web static assets and API
endpoints. Block 16 documents rationale for iterative refinement: first stabilize auth and
master data, then appointments, then doctor time slots, then polish UI flows.

2.17 Problem elaboration 17. The problem domain combines scheduling constraints (doctor
availability, slot conflicts) with identity management (unique emails, passwords) and
authorization (each role must not access others' administrative functions). The solution
must remain simple enough for a college project while demonstrating layered architecture,
RESTful design, validation, and clear separation between web static assets and API
endpoints. Block 17 documents rationale for iterative refinement: first stabilize auth and
master data, then appointments, then doctor time slots, then polish UI flows.

2.18 Problem elaboration 18. The problem domain combines scheduling constraints (doctor
availability, slot conflicts) with identity management (unique emails, passwords) and
authorization (each role must not access others' administrative functions). The solution
must remain simple enough for a college project while demonstrating layered architecture,
RESTful design, validation, and clear separation between web static assets and API
endpoints. Block 18 documents rationale for iterative refinement: first stabilize auth and
master data, then appointments, then doctor time slots, then polish UI flows.

2.19 Problem elaboration 19. The problem domain combines scheduling constraints (doctor
availability, slot conflicts) with identity management (unique emails, passwords) and
authorization (each role must not access others' administrative functions). The solution
must remain simple enough for a college project while demonstrating layered architecture,
RESTful design, validation, and clear separation between web static assets and API
endpoints. Block 19 documents rationale for iterative refinement: first stabilize auth and
master data, then appointments, then doctor time slots, then polish UI flows.

2.20 Problem elaboration 20. The problem domain combines scheduling constraints (doctor
availability, slot conflicts) with identity management (unique emails, passwords) and
authorization (each role must not access others' administrative functions). The solution
must remain simple enough for a college project while demonstrating layered architecture,
RESTful design, validation, and clear separation between web static assets and API
endpoints. Block 20 documents rationale for iterative refinement: first stabilize auth and
master data, then appointments, then doctor time slots, then polish UI flows.

2.21 Problem elaboration 21. The problem domain combines scheduling constraints (doctor
availability, slot conflicts) with identity management (unique emails, passwords) and
authorization (each role must not access others' administrative functions). The solution
must remain simple enough for a college project while demonstrating layered architecture,
RESTful design, validation, and clear separation between web static assets and API
endpoints. Block 21 documents rationale for iterative refinement: first stabilize auth and
master data, then appointments, then doctor time slots, then polish UI flows.

5. SOFTWARE REQUIREMENTS SPECIFICATION (HIGH-LEVEL)
===================================================

FR-AUTH-01: System shall allow patient registration with name, email, password, optional phone.
FR-AUTH-02: System shall allow login for Admin, Doctor, Patient with role context.
FR-AUTH-03: System shall issue token on successful login; APIs require X-Auth-Token header.
FR-ADM-01: Admin shall create, update, delete departments.
FR-ADM-02: Admin shall create, update, delete doctors linked to departments.
FR-ADM-03: Admin shall list/view patients and appointments as per implementation.
FR-DOC-01: Doctor shall list appointments assigned to them.
FR-DOC-02: Doctor shall update appointment status (PENDING/APPROVED/COMPLETED/CANCELLED).
FR-DOC-03: Doctor shall fetch patient details for a given appointment.
FR-DOC-04: Doctor shall manage time slots for availability.
FR-PAT-01: Patient shall list departments and doctors by department.
FR-PAT-02: Patient shall book and list own appointments; cancel as supported.
FR-PAT-03: Patient shall update profile.
FR-PUB-01: Public GET /api/departments/list and GET /api/doctors/by-department.

NFR-001: Non-functional requirement placeholder 1. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-002: Non-functional requirement placeholder 2. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-003: Non-functional requirement placeholder 3. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-004: Non-functional requirement placeholder 4. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-005: Non-functional requirement placeholder 5. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-006: Non-functional requirement placeholder 6. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-007: Non-functional requirement placeholder 7. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-008: Non-functional requirement placeholder 8. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-009: Non-functional requirement placeholder 9. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-010: Non-functional requirement placeholder 10. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-011: Non-functional requirement placeholder 11. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-012: Non-functional requirement placeholder 12. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-013: Non-functional requirement placeholder 13. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-014: Non-functional requirement placeholder 14. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-015: Non-functional requirement placeholder 15. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-016: Non-functional requirement placeholder 16. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-017: Non-functional requirement placeholder 17. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-018: Non-functional requirement placeholder 18. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-019: Non-functional requirement placeholder 19. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-020: Non-functional requirement placeholder 20. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-021: Non-functional requirement placeholder 21. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-022: Non-functional requirement placeholder 22. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-023: Non-functional requirement placeholder 23. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-024: Non-functional requirement placeholder 24. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-025: Non-functional requirement placeholder 25. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-026: Non-functional requirement placeholder 26. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-027: Non-functional requirement placeholder 27. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-028: Non-functional requirement placeholder 28. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-029: Non-functional requirement placeholder 29. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-030: Non-functional requirement placeholder 30. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-031: Non-functional requirement placeholder 31. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-032: Non-functional requirement placeholder 32. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-033: Non-functional requirement placeholder 33. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-034: Non-functional requirement placeholder 34. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-035: Non-functional requirement placeholder 35. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-036: Non-functional requirement placeholder 36. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-037: Non-functional requirement placeholder 37. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-038: Non-functional requirement placeholder 38. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-039: Non-functional requirement placeholder 39. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-040: Non-functional requirement placeholder 40. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-041: Non-functional requirement placeholder 41. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-042: Non-functional requirement placeholder 42. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-043: Non-functional requirement placeholder 43. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-044: Non-functional requirement placeholder 44. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-045: Non-functional requirement placeholder 45. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-046: Non-functional requirement placeholder 46. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-047: Non-functional requirement placeholder 47. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-048: Non-functional requirement placeholder 48. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-049: Non-functional requirement placeholder 49. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-050: Non-functional requirement placeholder 50. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-051: Non-functional requirement placeholder 51. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-052: Non-functional requirement placeholder 52. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-053: Non-functional requirement placeholder 53. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-054: Non-functional requirement placeholder 54. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-055: Non-functional requirement placeholder 55. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-056: Non-functional requirement placeholder 56. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-057: Non-functional requirement placeholder 57. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-058: Non-functional requirement placeholder 58. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-059: Non-functional requirement placeholder 59. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-060: Non-functional requirement placeholder 60. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-061: Non-functional requirement placeholder 61. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-062: Non-functional requirement placeholder 62. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-063: Non-functional requirement placeholder 63. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.
NFR-064: Non-functional requirement placeholder 64. Usability: forms must validate input and show errors. Security: tokens must not be logged in production; passwords must not appear in client storage. Performance: typical campus demo load is small; production would add connection pooling tuning. Maintainability: packages follow controller/service/repository separation. Portability: Spring Boot fat JAR runs wherever JVM and MySQL exist.

6. EXPANDED USE CASE NARRATIVES (BULK)
======================================

UC-STATUS-001: Doctor updates appointment status — Primary actor: Doctor. Success: Status transitions per rules.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-SLOT-002: Doctor defines time slot — Primary actor: Doctor. Success: Slot stored and linked to doctor.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-DEPT-003: Admin manages department — Primary actor: Admin. Success: Department master updated.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-DOC-004: Admin manages doctor profile — Primary actor: Admin. Success: Doctor directory updated.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-REG-005: Patient registers — Primary actor: Patient. Success: Patient row created with unique email.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-LOGIN-006: Any role logs in — Primary actor: Any. Success: Token returned for API use.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-BOOK-007: Patient books appointment — Primary actor: Patient. Success: Appointment record created PENDING.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-STATUS-008: Doctor updates appointment status — Primary actor: Doctor. Success: Status transitions per rules.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-SLOT-009: Doctor defines time slot — Primary actor: Doctor. Success: Slot stored and linked to doctor.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-DEPT-010: Admin manages department — Primary actor: Admin. Success: Department master updated.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-DOC-011: Admin manages doctor profile — Primary actor: Admin. Success: Doctor directory updated.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-REG-012: Patient registers — Primary actor: Patient. Success: Patient row created with unique email.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-LOGIN-013: Any role logs in — Primary actor: Any. Success: Token returned for API use.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-BOOK-014: Patient books appointment — Primary actor: Patient. Success: Appointment record created PENDING.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-STATUS-015: Doctor updates appointment status — Primary actor: Doctor. Success: Status transitions per rules.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-SLOT-016: Doctor defines time slot — Primary actor: Doctor. Success: Slot stored and linked to doctor.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-DEPT-017: Admin manages department — Primary actor: Admin. Success: Department master updated.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-DOC-018: Admin manages doctor profile — Primary actor: Admin. Success: Doctor directory updated.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-REG-019: Patient registers — Primary actor: Patient. Success: Patient row created with unique email.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-LOGIN-020: Any role logs in — Primary actor: Any. Success: Token returned for API use.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-BOOK-021: Patient books appointment — Primary actor: Patient. Success: Appointment record created PENDING.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-STATUS-022: Doctor updates appointment status — Primary actor: Doctor. Success: Status transitions per rules.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-SLOT-023: Doctor defines time slot — Primary actor: Doctor. Success: Slot stored and linked to doctor.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-DEPT-024: Admin manages department — Primary actor: Admin. Success: Department master updated.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-DOC-025: Admin manages doctor profile — Primary actor: Admin. Success: Doctor directory updated.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-REG-026: Patient registers — Primary actor: Patient. Success: Patient row created with unique email.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-LOGIN-027: Any role logs in — Primary actor: Any. Success: Token returned for API use.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-BOOK-028: Patient books appointment — Primary actor: Patient. Success: Appointment record created PENDING.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-STATUS-029: Doctor updates appointment status — Primary actor: Doctor. Success: Status transitions per rules.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-SLOT-030: Doctor defines time slot — Primary actor: Doctor. Success: Slot stored and linked to doctor.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-DEPT-031: Admin manages department — Primary actor: Admin. Success: Department master updated.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-DOC-032: Admin manages doctor profile — Primary actor: Admin. Success: Doctor directory updated.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-REG-033: Patient registers — Primary actor: Patient. Success: Patient row created with unique email.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-LOGIN-034: Any role logs in — Primary actor: Any. Success: Token returned for API use.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-BOOK-035: Patient books appointment — Primary actor: Patient. Success: Appointment record created PENDING.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-STATUS-036: Doctor updates appointment status — Primary actor: Doctor. Success: Status transitions per rules.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-SLOT-037: Doctor defines time slot — Primary actor: Doctor. Success: Slot stored and linked to doctor.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-DEPT-038: Admin manages department — Primary actor: Admin. Success: Department master updated.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-DOC-039: Admin manages doctor profile — Primary actor: Admin. Success: Doctor directory updated.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-REG-040: Patient registers — Primary actor: Patient. Success: Patient row created with unique email.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-LOGIN-041: Any role logs in — Primary actor: Any. Success: Token returned for API use.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-BOOK-042: Patient books appointment — Primary actor: Patient. Success: Appointment record created PENDING.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-STATUS-043: Doctor updates appointment status — Primary actor: Doctor. Success: Status transitions per rules.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-SLOT-044: Doctor defines time slot — Primary actor: Doctor. Success: Slot stored and linked to doctor.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-DEPT-045: Admin manages department — Primary actor: Admin. Success: Department master updated.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-DOC-046: Admin manages doctor profile — Primary actor: Admin. Success: Doctor directory updated.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-REG-047: Patient registers — Primary actor: Patient. Success: Patient row created with unique email.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-LOGIN-048: Any role logs in — Primary actor: Any. Success: Token returned for API use.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-BOOK-049: Patient books appointment — Primary actor: Patient. Success: Appointment record created PENDING.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-STATUS-050: Doctor updates appointment status — Primary actor: Doctor. Success: Status transitions per rules.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-SLOT-051: Doctor defines time slot — Primary actor: Doctor. Success: Slot stored and linked to doctor.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-DEPT-052: Admin manages department — Primary actor: Admin. Success: Department master updated.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-DOC-053: Admin manages doctor profile — Primary actor: Admin. Success: Doctor directory updated.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-REG-054: Patient registers — Primary actor: Patient. Success: Patient row created with unique email.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-LOGIN-055: Any role logs in — Primary actor: Any. Success: Token returned for API use.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-BOOK-056: Patient books appointment — Primary actor: Patient. Success: Appointment record created PENDING.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-STATUS-057: Doctor updates appointment status — Primary actor: Doctor. Success: Status transitions per rules.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-SLOT-058: Doctor defines time slot — Primary actor: Doctor. Success: Slot stored and linked to doctor.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-DEPT-059: Admin manages department — Primary actor: Admin. Success: Department master updated.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-DOC-060: Admin manages doctor profile — Primary actor: Admin. Success: Doctor directory updated.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-REG-061: Patient registers — Primary actor: Patient. Success: Patient row created with unique email.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-LOGIN-062: Any role logs in — Primary actor: Any. Success: Token returned for API use.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-BOOK-063: Patient books appointment — Primary actor: Patient. Success: Appointment record created PENDING.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-STATUS-064: Doctor updates appointment status — Primary actor: Doctor. Success: Status transitions per rules.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-SLOT-065: Doctor defines time slot — Primary actor: Doctor. Success: Slot stored and linked to doctor.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-DEPT-066: Admin manages department — Primary actor: Admin. Success: Department master updated.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-DOC-067: Admin manages doctor profile — Primary actor: Admin. Success: Doctor directory updated.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-REG-068: Patient registers — Primary actor: Patient. Success: Patient row created with unique email.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-LOGIN-069: Any role logs in — Primary actor: Any. Success: Token returned for API use.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-BOOK-070: Patient books appointment — Primary actor: Patient. Success: Appointment record created PENDING.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-STATUS-071: Doctor updates appointment status — Primary actor: Doctor. Success: Status transitions per rules.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-SLOT-072: Doctor defines time slot — Primary actor: Doctor. Success: Slot stored and linked to doctor.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-DEPT-073: Admin manages department — Primary actor: Admin. Success: Department master updated.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-DOC-074: Admin manages doctor profile — Primary actor: Admin. Success: Doctor directory updated.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-REG-075: Patient registers — Primary actor: Patient. Success: Patient row created with unique email.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-LOGIN-076: Any role logs in — Primary actor: Any. Success: Token returned for API use.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-BOOK-077: Patient books appointment — Primary actor: Patient. Success: Appointment record created PENDING.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-STATUS-078: Doctor updates appointment status — Primary actor: Doctor. Success: Status transitions per rules.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-SLOT-079: Doctor defines time slot — Primary actor: Doctor. Success: Slot stored and linked to doctor.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-DEPT-080: Admin manages department — Primary actor: Admin. Success: Department master updated.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-DOC-081: Admin manages doctor profile — Primary actor: Admin. Success: Doctor directory updated.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-REG-082: Patient registers — Primary actor: Patient. Success: Patient row created with unique email.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-LOGIN-083: Any role logs in — Primary actor: Any. Success: Token returned for API use.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-BOOK-084: Patient books appointment — Primary actor: Patient. Success: Appointment record created PENDING.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-STATUS-085: Doctor updates appointment status — Primary actor: Doctor. Success: Status transitions per rules.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-SLOT-086: Doctor defines time slot — Primary actor: Doctor. Success: Slot stored and linked to doctor.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-DEPT-087: Admin manages department — Primary actor: Admin. Success: Department master updated.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-DOC-088: Admin manages doctor profile — Primary actor: Admin. Success: Doctor directory updated.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-REG-089: Patient registers — Primary actor: Patient. Success: Patient row created with unique email.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-LOGIN-090: Any role logs in — Primary actor: Any. Success: Token returned for API use.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-BOOK-091: Patient books appointment — Primary actor: Patient. Success: Appointment record created PENDING.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-STATUS-092: Doctor updates appointment status — Primary actor: Doctor. Success: Status transitions per rules.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-SLOT-093: Doctor defines time slot — Primary actor: Doctor. Success: Slot stored and linked to doctor.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-DEPT-094: Admin manages department — Primary actor: Admin. Success: Department master updated.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-DOC-095: Admin manages doctor profile — Primary actor: Admin. Success: Doctor directory updated.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-REG-096: Patient registers — Primary actor: Patient. Success: Patient row created with unique email.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-LOGIN-097: Any role logs in — Primary actor: Any. Success: Token returned for API use.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-BOOK-098: Patient books appointment — Primary actor: Patient. Success: Appointment record created PENDING.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-STATUS-099: Doctor updates appointment status — Primary actor: Doctor. Success: Status transitions per rules.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-SLOT-100: Doctor defines time slot — Primary actor: Doctor. Success: Slot stored and linked to doctor.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-DEPT-101: Admin manages department — Primary actor: Admin. Success: Department master updated.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-DOC-102: Admin manages doctor profile — Primary actor: Admin. Success: Doctor directory updated.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-REG-103: Patient registers — Primary actor: Patient. Success: Patient row created with unique email.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-LOGIN-104: Any role logs in — Primary actor: Any. Success: Token returned for API use.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-BOOK-105: Patient books appointment — Primary actor: Patient. Success: Appointment record created PENDING.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-STATUS-106: Doctor updates appointment status — Primary actor: Doctor. Success: Status transitions per rules.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-SLOT-107: Doctor defines time slot — Primary actor: Doctor. Success: Slot stored and linked to doctor.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-DEPT-108: Admin manages department — Primary actor: Admin. Success: Department master updated.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

UC-DOC-109: Admin manages doctor profile — Primary actor: Admin. Success: Doctor directory updated.
  Preconditions: valid DB, application running, network available.
  Main flow: (1) authenticate if needed (2) navigate UI or call API (3) submit valid data 
  (4) server validates (5) persistence via JPA (6) response to client.
  Alternate flows: invalid email format, duplicate registration, missing token, wrong role.
  Postconditions: audit-friendly state in DB; UI refresh shows new data.

10. API ENDPOINT EXPANSIONS (NARRATIVE)
=======================================

API-NOTE-1: Endpoint family `POST /api/auth/register`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 1 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-2: Endpoint family `POST /api/auth/logout`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 2 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-3: Endpoint family `/api/admin/departments`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 3 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-4: Endpoint family `/api/admin/doctors`. In a report, each endpoint should document
HTTP method, path parameters, request body schema, response schema (DTO), success codes
(200/201), client errors (400), auth errors (401/403), and server errors (500). For this
project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 4 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-5: Endpoint family `/api/admin/patients`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 5 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-6: Endpoint family `/api/admin/appointments`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 6 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-7: Endpoint family `/api/doctor/appointments`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 7 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-8: Endpoint family `/api/doctor/appointments/{id}/status`. In a report, each
endpoint should document HTTP method, path parameters, request body schema, response schema
(DTO), success codes (200/201), client errors (400), auth errors (401/403), and server
errors (500). For this project, the frontend uses fetch/XHR with JSON and attaches
X-Auth-Token after login. Students should paste Postman screenshots in the PDF for key
flows. Note 8 emphasizes traceability from UI button → JS function → REST call → controller
→ service → repository → SQL.

API-NOTE-9: Endpoint family `/api/doctor/appointments/{id}/patient`. In a report, each
endpoint should document HTTP method, path parameters, request body schema, response schema
(DTO), success codes (200/201), client errors (400), auth errors (401/403), and server
errors (500). For this project, the frontend uses fetch/XHR with JSON and attaches
X-Auth-Token after login. Students should paste Postman screenshots in the PDF for key
flows. Note 9 emphasizes traceability from UI button → JS function → REST call → controller
→ service → repository → SQL.

API-NOTE-10: Endpoint family `/api/doctor/time-slots`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 10 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-11: Endpoint family `/api/patient/departments`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 11 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-12: Endpoint family `/api/patient/doctors/by-department/{id}`. In a report, each
endpoint should document HTTP method, path parameters, request body schema, response schema
(DTO), success codes (200/201), client errors (400), auth errors (401/403), and server
errors (500). For this project, the frontend uses fetch/XHR with JSON and attaches
X-Auth-Token after login. Students should paste Postman screenshots in the PDF for key
flows. Note 12 emphasizes traceability from UI button → JS function → REST call → controller
→ service → repository → SQL.

API-NOTE-13: Endpoint family `/api/patient/appointments`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 13 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-14: Endpoint family `/api/patient/profile`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 14 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-15: Endpoint family `GET /api/departments/list`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 15 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-16: Endpoint family `GET /api/doctors/by-department?departmentId=`. In a report,
each endpoint should document HTTP method, path parameters, request body schema, response
schema (DTO), success codes (200/201), client errors (400), auth errors (401/403), and
server errors (500). For this project, the frontend uses fetch/XHR with JSON and attaches
X-Auth-Token after login. Students should paste Postman screenshots in the PDF for key
flows. Note 16 emphasizes traceability from UI button → JS function → REST call → controller
→ service → repository → SQL.

API-NOTE-17: Endpoint family `POST /api/auth/login`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 17 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-18: Endpoint family `POST /api/auth/register`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 18 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-19: Endpoint family `POST /api/auth/logout`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 19 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-20: Endpoint family `/api/admin/departments`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 20 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-21: Endpoint family `/api/admin/doctors`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 21 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-22: Endpoint family `/api/admin/patients`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 22 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-23: Endpoint family `/api/admin/appointments`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 23 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-24: Endpoint family `/api/doctor/appointments`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 24 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-25: Endpoint family `/api/doctor/appointments/{id}/status`. In a report, each
endpoint should document HTTP method, path parameters, request body schema, response schema
(DTO), success codes (200/201), client errors (400), auth errors (401/403), and server
errors (500). For this project, the frontend uses fetch/XHR with JSON and attaches
X-Auth-Token after login. Students should paste Postman screenshots in the PDF for key
flows. Note 25 emphasizes traceability from UI button → JS function → REST call → controller
→ service → repository → SQL.

API-NOTE-26: Endpoint family `/api/doctor/appointments/{id}/patient`. In a report, each
endpoint should document HTTP method, path parameters, request body schema, response schema
(DTO), success codes (200/201), client errors (400), auth errors (401/403), and server
errors (500). For this project, the frontend uses fetch/XHR with JSON and attaches
X-Auth-Token after login. Students should paste Postman screenshots in the PDF for key
flows. Note 26 emphasizes traceability from UI button → JS function → REST call → controller
→ service → repository → SQL.

API-NOTE-27: Endpoint family `/api/doctor/time-slots`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 27 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-28: Endpoint family `/api/patient/departments`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 28 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-29: Endpoint family `/api/patient/doctors/by-department/{id}`. In a report, each
endpoint should document HTTP method, path parameters, request body schema, response schema
(DTO), success codes (200/201), client errors (400), auth errors (401/403), and server
errors (500). For this project, the frontend uses fetch/XHR with JSON and attaches
X-Auth-Token after login. Students should paste Postman screenshots in the PDF for key
flows. Note 29 emphasizes traceability from UI button → JS function → REST call → controller
→ service → repository → SQL.

API-NOTE-30: Endpoint family `/api/patient/appointments`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 30 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-31: Endpoint family `/api/patient/profile`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 31 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-32: Endpoint family `GET /api/departments/list`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 32 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-33: Endpoint family `GET /api/doctors/by-department?departmentId=`. In a report,
each endpoint should document HTTP method, path parameters, request body schema, response
schema (DTO), success codes (200/201), client errors (400), auth errors (401/403), and
server errors (500). For this project, the frontend uses fetch/XHR with JSON and attaches
X-Auth-Token after login. Students should paste Postman screenshots in the PDF for key
flows. Note 33 emphasizes traceability from UI button → JS function → REST call → controller
→ service → repository → SQL.

API-NOTE-34: Endpoint family `POST /api/auth/login`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 34 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-35: Endpoint family `POST /api/auth/register`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 35 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-36: Endpoint family `POST /api/auth/logout`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 36 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-37: Endpoint family `/api/admin/departments`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 37 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-38: Endpoint family `/api/admin/doctors`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 38 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-39: Endpoint family `/api/admin/patients`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 39 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-40: Endpoint family `/api/admin/appointments`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 40 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-41: Endpoint family `/api/doctor/appointments`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 41 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-42: Endpoint family `/api/doctor/appointments/{id}/status`. In a report, each
endpoint should document HTTP method, path parameters, request body schema, response schema
(DTO), success codes (200/201), client errors (400), auth errors (401/403), and server
errors (500). For this project, the frontend uses fetch/XHR with JSON and attaches
X-Auth-Token after login. Students should paste Postman screenshots in the PDF for key
flows. Note 42 emphasizes traceability from UI button → JS function → REST call → controller
→ service → repository → SQL.

API-NOTE-43: Endpoint family `/api/doctor/appointments/{id}/patient`. In a report, each
endpoint should document HTTP method, path parameters, request body schema, response schema
(DTO), success codes (200/201), client errors (400), auth errors (401/403), and server
errors (500). For this project, the frontend uses fetch/XHR with JSON and attaches
X-Auth-Token after login. Students should paste Postman screenshots in the PDF for key
flows. Note 43 emphasizes traceability from UI button → JS function → REST call → controller
→ service → repository → SQL.

API-NOTE-44: Endpoint family `/api/doctor/time-slots`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 44 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-45: Endpoint family `/api/patient/departments`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 45 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-46: Endpoint family `/api/patient/doctors/by-department/{id}`. In a report, each
endpoint should document HTTP method, path parameters, request body schema, response schema
(DTO), success codes (200/201), client errors (400), auth errors (401/403), and server
errors (500). For this project, the frontend uses fetch/XHR with JSON and attaches
X-Auth-Token after login. Students should paste Postman screenshots in the PDF for key
flows. Note 46 emphasizes traceability from UI button → JS function → REST call → controller
→ service → repository → SQL.

API-NOTE-47: Endpoint family `/api/patient/appointments`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 47 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-48: Endpoint family `/api/patient/profile`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 48 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-49: Endpoint family `GET /api/departments/list`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 49 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-50: Endpoint family `GET /api/doctors/by-department?departmentId=`. In a report,
each endpoint should document HTTP method, path parameters, request body schema, response
schema (DTO), success codes (200/201), client errors (400), auth errors (401/403), and
server errors (500). For this project, the frontend uses fetch/XHR with JSON and attaches
X-Auth-Token after login. Students should paste Postman screenshots in the PDF for key
flows. Note 50 emphasizes traceability from UI button → JS function → REST call → controller
→ service → repository → SQL.

API-NOTE-51: Endpoint family `POST /api/auth/login`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 51 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-52: Endpoint family `POST /api/auth/register`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 52 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-53: Endpoint family `POST /api/auth/logout`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 53 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-54: Endpoint family `/api/admin/departments`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 54 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-55: Endpoint family `/api/admin/doctors`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 55 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-56: Endpoint family `/api/admin/patients`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 56 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-57: Endpoint family `/api/admin/appointments`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 57 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-58: Endpoint family `/api/doctor/appointments`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 58 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-59: Endpoint family `/api/doctor/appointments/{id}/status`. In a report, each
endpoint should document HTTP method, path parameters, request body schema, response schema
(DTO), success codes (200/201), client errors (400), auth errors (401/403), and server
errors (500). For this project, the frontend uses fetch/XHR with JSON and attaches
X-Auth-Token after login. Students should paste Postman screenshots in the PDF for key
flows. Note 59 emphasizes traceability from UI button → JS function → REST call → controller
→ service → repository → SQL.

API-NOTE-60: Endpoint family `/api/doctor/appointments/{id}/patient`. In a report, each
endpoint should document HTTP method, path parameters, request body schema, response schema
(DTO), success codes (200/201), client errors (400), auth errors (401/403), and server
errors (500). For this project, the frontend uses fetch/XHR with JSON and attaches
X-Auth-Token after login. Students should paste Postman screenshots in the PDF for key
flows. Note 60 emphasizes traceability from UI button → JS function → REST call → controller
→ service → repository → SQL.

API-NOTE-61: Endpoint family `/api/doctor/time-slots`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 61 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-62: Endpoint family `/api/patient/departments`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 62 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-63: Endpoint family `/api/patient/doctors/by-department/{id}`. In a report, each
endpoint should document HTTP method, path parameters, request body schema, response schema
(DTO), success codes (200/201), client errors (400), auth errors (401/403), and server
errors (500). For this project, the frontend uses fetch/XHR with JSON and attaches
X-Auth-Token after login. Students should paste Postman screenshots in the PDF for key
flows. Note 63 emphasizes traceability from UI button → JS function → REST call → controller
→ service → repository → SQL.

API-NOTE-64: Endpoint family `/api/patient/appointments`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 64 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-65: Endpoint family `/api/patient/profile`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 65 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-66: Endpoint family `GET /api/departments/list`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 66 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-67: Endpoint family `GET /api/doctors/by-department?departmentId=`. In a report,
each endpoint should document HTTP method, path parameters, request body schema, response
schema (DTO), success codes (200/201), client errors (400), auth errors (401/403), and
server errors (500). For this project, the frontend uses fetch/XHR with JSON and attaches
X-Auth-Token after login. Students should paste Postman screenshots in the PDF for key
flows. Note 67 emphasizes traceability from UI button → JS function → REST call → controller
→ service → repository → SQL.

API-NOTE-68: Endpoint family `POST /api/auth/login`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 68 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-69: Endpoint family `POST /api/auth/register`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 69 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-70: Endpoint family `POST /api/auth/logout`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 70 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-71: Endpoint family `/api/admin/departments`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 71 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-72: Endpoint family `/api/admin/doctors`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 72 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-73: Endpoint family `/api/admin/patients`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 73 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-74: Endpoint family `/api/admin/appointments`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 74 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-75: Endpoint family `/api/doctor/appointments`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 75 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-76: Endpoint family `/api/doctor/appointments/{id}/status`. In a report, each
endpoint should document HTTP method, path parameters, request body schema, response schema
(DTO), success codes (200/201), client errors (400), auth errors (401/403), and server
errors (500). For this project, the frontend uses fetch/XHR with JSON and attaches
X-Auth-Token after login. Students should paste Postman screenshots in the PDF for key
flows. Note 76 emphasizes traceability from UI button → JS function → REST call → controller
→ service → repository → SQL.

API-NOTE-77: Endpoint family `/api/doctor/appointments/{id}/patient`. In a report, each
endpoint should document HTTP method, path parameters, request body schema, response schema
(DTO), success codes (200/201), client errors (400), auth errors (401/403), and server
errors (500). For this project, the frontend uses fetch/XHR with JSON and attaches
X-Auth-Token after login. Students should paste Postman screenshots in the PDF for key
flows. Note 77 emphasizes traceability from UI button → JS function → REST call → controller
→ service → repository → SQL.

API-NOTE-78: Endpoint family `/api/doctor/time-slots`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 78 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-79: Endpoint family `/api/patient/departments`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 79 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-80: Endpoint family `/api/patient/doctors/by-department/{id}`. In a report, each
endpoint should document HTTP method, path parameters, request body schema, response schema
(DTO), success codes (200/201), client errors (400), auth errors (401/403), and server
errors (500). For this project, the frontend uses fetch/XHR with JSON and attaches
X-Auth-Token after login. Students should paste Postman screenshots in the PDF for key
flows. Note 80 emphasizes traceability from UI button → JS function → REST call → controller
→ service → repository → SQL.

API-NOTE-81: Endpoint family `/api/patient/appointments`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 81 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-82: Endpoint family `/api/patient/profile`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 82 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-83: Endpoint family `GET /api/departments/list`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 83 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-84: Endpoint family `GET /api/doctors/by-department?departmentId=`. In a report,
each endpoint should document HTTP method, path parameters, request body schema, response
schema (DTO), success codes (200/201), client errors (400), auth errors (401/403), and
server errors (500). For this project, the frontend uses fetch/XHR with JSON and attaches
X-Auth-Token after login. Students should paste Postman screenshots in the PDF for key
flows. Note 84 emphasizes traceability from UI button → JS function → REST call → controller
→ service → repository → SQL.

API-NOTE-85: Endpoint family `POST /api/auth/login`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 85 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-86: Endpoint family `POST /api/auth/register`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 86 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-87: Endpoint family `POST /api/auth/logout`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 87 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-88: Endpoint family `/api/admin/departments`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 88 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-89: Endpoint family `/api/admin/doctors`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 89 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-90: Endpoint family `/api/admin/patients`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 90 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-91: Endpoint family `/api/admin/appointments`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 91 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-92: Endpoint family `/api/doctor/appointments`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 92 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-93: Endpoint family `/api/doctor/appointments/{id}/status`. In a report, each
endpoint should document HTTP method, path parameters, request body schema, response schema
(DTO), success codes (200/201), client errors (400), auth errors (401/403), and server
errors (500). For this project, the frontend uses fetch/XHR with JSON and attaches
X-Auth-Token after login. Students should paste Postman screenshots in the PDF for key
flows. Note 93 emphasizes traceability from UI button → JS function → REST call → controller
→ service → repository → SQL.

API-NOTE-94: Endpoint family `/api/doctor/appointments/{id}/patient`. In a report, each
endpoint should document HTTP method, path parameters, request body schema, response schema
(DTO), success codes (200/201), client errors (400), auth errors (401/403), and server
errors (500). For this project, the frontend uses fetch/XHR with JSON and attaches
X-Auth-Token after login. Students should paste Postman screenshots in the PDF for key
flows. Note 94 emphasizes traceability from UI button → JS function → REST call → controller
→ service → repository → SQL.

API-NOTE-95: Endpoint family `/api/doctor/time-slots`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 95 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-96: Endpoint family `/api/patient/departments`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 96 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-97: Endpoint family `/api/patient/doctors/by-department/{id}`. In a report, each
endpoint should document HTTP method, path parameters, request body schema, response schema
(DTO), success codes (200/201), client errors (400), auth errors (401/403), and server
errors (500). For this project, the frontend uses fetch/XHR with JSON and attaches
X-Auth-Token after login. Students should paste Postman screenshots in the PDF for key
flows. Note 97 emphasizes traceability from UI button → JS function → REST call → controller
→ service → repository → SQL.

API-NOTE-98: Endpoint family `/api/patient/appointments`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 98 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-99: Endpoint family `/api/patient/profile`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 99 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-100: Endpoint family `GET /api/departments/list`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 100 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-101: Endpoint family `GET /api/doctors/by-department?departmentId=`. In a report,
each endpoint should document HTTP method, path parameters, request body schema, response
schema (DTO), success codes (200/201), client errors (400), auth errors (401/403), and
server errors (500). For this project, the frontend uses fetch/XHR with JSON and attaches
X-Auth-Token after login. Students should paste Postman screenshots in the PDF for key
flows. Note 101 emphasizes traceability from UI button → JS function → REST call →
controller → service → repository → SQL.

API-NOTE-102: Endpoint family `POST /api/auth/login`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 102 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-103: Endpoint family `POST /api/auth/register`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 103 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-104: Endpoint family `POST /api/auth/logout`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 104 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-105: Endpoint family `/api/admin/departments`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 105 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-106: Endpoint family `/api/admin/doctors`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 106 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-107: Endpoint family `/api/admin/patients`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 107 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-108: Endpoint family `/api/admin/appointments`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 108 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-109: Endpoint family `/api/doctor/appointments`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 109 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-110: Endpoint family `/api/doctor/appointments/{id}/status`. In a report, each
endpoint should document HTTP method, path parameters, request body schema, response schema
(DTO), success codes (200/201), client errors (400), auth errors (401/403), and server
errors (500). For this project, the frontend uses fetch/XHR with JSON and attaches
X-Auth-Token after login. Students should paste Postman screenshots in the PDF for key
flows. Note 110 emphasizes traceability from UI button → JS function → REST call →
controller → service → repository → SQL.

API-NOTE-111: Endpoint family `/api/doctor/appointments/{id}/patient`. In a report, each
endpoint should document HTTP method, path parameters, request body schema, response schema
(DTO), success codes (200/201), client errors (400), auth errors (401/403), and server
errors (500). For this project, the frontend uses fetch/XHR with JSON and attaches
X-Auth-Token after login. Students should paste Postman screenshots in the PDF for key
flows. Note 111 emphasizes traceability from UI button → JS function → REST call →
controller → service → repository → SQL.

API-NOTE-112: Endpoint family `/api/doctor/time-slots`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 112 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-113: Endpoint family `/api/patient/departments`. In a report, each endpoint should
document HTTP method, path parameters, request body schema, response schema (DTO), success
codes (200/201), client errors (400), auth errors (401/403), and server errors (500). For
this project, the frontend uses fetch/XHR with JSON and attaches X-Auth-Token after login.
Students should paste Postman screenshots in the PDF for key flows. Note 113 emphasizes
traceability from UI button → JS function → REST call → controller → service → repository →
SQL.

API-NOTE-114: Endpoint family `/api/patient/doctors/by-department/{id}`. In a report, each
endpoint should document HTTP method, path parameters, request body schema, response schema
(DTO), success codes (200/201), client errors (400), auth errors (401/403), and server
errors (500). For this project, the frontend uses fetch/XHR with JSON and attaches
X-Auth-Token after login. Students should paste Postman screenshots in the PDF for key
flows. Note 114 emphasizes traceability from UI button → JS function → REST call →
controller → service → repository → SQL.

12. DATABASE — TABLES (PROJECT-ALIGNED)
=======================================

patients: patient_id PK, name, email UK, password, phone nullable; 1-to-many appointments.
doctors: doctor_id PK, name, email UK, password, department_id FK, specialization, availability;
  one department; 1-to-many appointments and time_slots.
departments: id PK, name, description (per entity file).
admins: id PK, name, email UK, password (per entity file).
appointments: appointment_id PK; patient_id FK; doctor_id FK; date; time; status enum string.
  Status values in code: PENDING, APPROVED, COMPLETED, CANCELLED.
time_slots: slot PK; doctor_id FK; slot_date; start_time; end_time; available flag.

12.1 Normalization discussion block 1. Each table represents one logical entity with minimal
redundant repeating groups. Doctor–department many-to-one avoids duplicating department name
per doctor row. Appointments reference patient and doctor by key rather than storing
denormalized names as authoritative sources (display names can be joined in queries or DTO
mapping). This block 1 can accompany textbook definitions of 1NF/2NF/3NF; for viva, explain
why appointment is its own table instead of embedding booking inside patient as a JSON blob
in SQL.

12.2 Normalization discussion block 2. Each table represents one logical entity with minimal
redundant repeating groups. Doctor–department many-to-one avoids duplicating department name
per doctor row. Appointments reference patient and doctor by key rather than storing
denormalized names as authoritative sources (display names can be joined in queries or DTO
mapping). This block 2 can accompany textbook definitions of 1NF/2NF/3NF; for viva, explain
why appointment is its own table instead of embedding booking inside patient as a JSON blob
in SQL.

12.3 Normalization discussion block 3. Each table represents one logical entity with minimal
redundant repeating groups. Doctor–department many-to-one avoids duplicating department name
per doctor row. Appointments reference patient and doctor by key rather than storing
denormalized names as authoritative sources (display names can be joined in queries or DTO
mapping). This block 3 can accompany textbook definitions of 1NF/2NF/3NF; for viva, explain
why appointment is its own table instead of embedding booking inside patient as a JSON blob
in SQL.

12.4 Normalization discussion block 4. Each table represents one logical entity with minimal
redundant repeating groups. Doctor–department many-to-one avoids duplicating department name
per doctor row. Appointments reference patient and doctor by key rather than storing
denormalized names as authoritative sources (display names can be joined in queries or DTO
mapping). This block 4 can accompany textbook definitions of 1NF/2NF/3NF; for viva, explain
why appointment is its own table instead of embedding booking inside patient as a JSON blob
in SQL.

12.5 Normalization discussion block 5. Each table represents one logical entity with minimal
redundant repeating groups. Doctor–department many-to-one avoids duplicating department name
per doctor row. Appointments reference patient and doctor by key rather than storing
denormalized names as authoritative sources (display names can be joined in queries or DTO
mapping). This block 5 can accompany textbook definitions of 1NF/2NF/3NF; for viva, explain
why appointment is its own table instead of embedding booking inside patient as a JSON blob
in SQL.

12.6 Normalization discussion block 6. Each table represents one logical entity with minimal
redundant repeating groups. Doctor–department many-to-one avoids duplicating department name
per doctor row. Appointments reference patient and doctor by key rather than storing
denormalized names as authoritative sources (display names can be joined in queries or DTO
mapping). This block 6 can accompany textbook definitions of 1NF/2NF/3NF; for viva, explain
why appointment is its own table instead of embedding booking inside patient as a JSON blob
in SQL.

12.7 Normalization discussion block 7. Each table represents one logical entity with minimal
redundant repeating groups. Doctor–department many-to-one avoids duplicating department name
per doctor row. Appointments reference patient and doctor by key rather than storing
denormalized names as authoritative sources (display names can be joined in queries or DTO
mapping). This block 7 can accompany textbook definitions of 1NF/2NF/3NF; for viva, explain
why appointment is its own table instead of embedding booking inside patient as a JSON blob
in SQL.

12.8 Normalization discussion block 8. Each table represents one logical entity with minimal
redundant repeating groups. Doctor–department many-to-one avoids duplicating department name
per doctor row. Appointments reference patient and doctor by key rather than storing
denormalized names as authoritative sources (display names can be joined in queries or DTO
mapping). This block 8 can accompany textbook definitions of 1NF/2NF/3NF; for viva, explain
why appointment is its own table instead of embedding booking inside patient as a JSON blob
in SQL.

12.9 Normalization discussion block 9. Each table represents one logical entity with minimal
redundant repeating groups. Doctor–department many-to-one avoids duplicating department name
per doctor row. Appointments reference patient and doctor by key rather than storing
denormalized names as authoritative sources (display names can be joined in queries or DTO
mapping). This block 9 can accompany textbook definitions of 1NF/2NF/3NF; for viva, explain
why appointment is its own table instead of embedding booking inside patient as a JSON blob
in SQL.

12.10 Normalization discussion block 10. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 10 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.11 Normalization discussion block 11. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 11 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.12 Normalization discussion block 12. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 12 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.13 Normalization discussion block 13. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 13 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.14 Normalization discussion block 14. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 14 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.15 Normalization discussion block 15. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 15 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.16 Normalization discussion block 16. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 16 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.17 Normalization discussion block 17. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 17 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.18 Normalization discussion block 18. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 18 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.19 Normalization discussion block 19. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 19 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.20 Normalization discussion block 20. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 20 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.21 Normalization discussion block 21. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 21 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.22 Normalization discussion block 22. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 22 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.23 Normalization discussion block 23. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 23 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.24 Normalization discussion block 24. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 24 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.25 Normalization discussion block 25. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 25 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.26 Normalization discussion block 26. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 26 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.27 Normalization discussion block 27. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 27 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.28 Normalization discussion block 28. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 28 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.29 Normalization discussion block 29. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 29 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.30 Normalization discussion block 30. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 30 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.31 Normalization discussion block 31. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 31 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.32 Normalization discussion block 32. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 32 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.33 Normalization discussion block 33. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 33 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.34 Normalization discussion block 34. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 34 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.35 Normalization discussion block 35. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 35 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.36 Normalization discussion block 36. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 36 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.37 Normalization discussion block 37. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 37 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.38 Normalization discussion block 38. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 38 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.39 Normalization discussion block 39. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 39 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.40 Normalization discussion block 40. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 40 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.41 Normalization discussion block 41. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 41 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.42 Normalization discussion block 42. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 42 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.43 Normalization discussion block 43. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 43 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.44 Normalization discussion block 44. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 44 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.45 Normalization discussion block 45. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 45 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.46 Normalization discussion block 46. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 46 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.47 Normalization discussion block 47. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 47 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.48 Normalization discussion block 48. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 48 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.49 Normalization discussion block 49. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 49 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.50 Normalization discussion block 50. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 50 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.51 Normalization discussion block 51. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 51 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.52 Normalization discussion block 52. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 52 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.53 Normalization discussion block 53. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 53 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.54 Normalization discussion block 54. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 54 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.55 Normalization discussion block 55. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 55 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.56 Normalization discussion block 56. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 56 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.57 Normalization discussion block 57. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 57 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.58 Normalization discussion block 58. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 58 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.59 Normalization discussion block 59. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 59 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.60 Normalization discussion block 60. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 60 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.61 Normalization discussion block 61. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 61 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.62 Normalization discussion block 62. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 62 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.63 Normalization discussion block 63. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 63 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.64 Normalization discussion block 64. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 64 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.65 Normalization discussion block 65. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 65 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.66 Normalization discussion block 66. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 66 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.67 Normalization discussion block 67. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 67 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.68 Normalization discussion block 68. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 68 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.69 Normalization discussion block 69. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 69 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.70 Normalization discussion block 70. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 70 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.71 Normalization discussion block 71. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 71 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.72 Normalization discussion block 72. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 72 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.73 Normalization discussion block 73. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 73 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

12.74 Normalization discussion block 74. Each table represents one logical entity with
minimal redundant repeating groups. Doctor–department many-to-one avoids duplicating
department name per doctor row. Appointments reference patient and doctor by key rather than
storing denormalized names as authoritative sources (display names can be joined in queries
or DTO mapping). This block 74 can accompany textbook definitions of 1NF/2NF/3NF; for viva,
explain why appointment is its own table instead of embedding booking inside patient as a
JSON blob in SQL.

14. SAMPLE TEST CASES (REPEATABLE GRID)
=======================================

TC-0001 | Module: 2 | Type: negative | Step: perform operation #1 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0002 | Module: 3 | Type: positive | Step: perform operation #2 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0003 | Module: 4 | Type: negative | Step: perform operation #3 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0004 | Module: 5 | Type: positive | Step: perform operation #4 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0005 | Module: 1 | Type: negative | Step: perform operation #5 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0006 | Module: 2 | Type: positive | Step: perform operation #6 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0007 | Module: 3 | Type: negative | Step: perform operation #7 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0008 | Module: 4 | Type: positive | Step: perform operation #8 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0009 | Module: 5 | Type: negative | Step: perform operation #9 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0010 | Module: 1 | Type: positive | Step: perform operation #10 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0011 | Module: 2 | Type: negative | Step: perform operation #11 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0012 | Module: 3 | Type: positive | Step: perform operation #12 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0013 | Module: 4 | Type: negative | Step: perform operation #13 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0014 | Module: 5 | Type: positive | Step: perform operation #14 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0015 | Module: 1 | Type: negative | Step: perform operation #15 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0016 | Module: 2 | Type: positive | Step: perform operation #16 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0017 | Module: 3 | Type: negative | Step: perform operation #17 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0018 | Module: 4 | Type: positive | Step: perform operation #18 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0019 | Module: 5 | Type: negative | Step: perform operation #19 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0020 | Module: 1 | Type: positive | Step: perform operation #20 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0021 | Module: 2 | Type: negative | Step: perform operation #21 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0022 | Module: 3 | Type: positive | Step: perform operation #22 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0023 | Module: 4 | Type: negative | Step: perform operation #23 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0024 | Module: 5 | Type: positive | Step: perform operation #24 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0025 | Module: 1 | Type: negative | Step: perform operation #25 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0026 | Module: 2 | Type: positive | Step: perform operation #26 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0027 | Module: 3 | Type: negative | Step: perform operation #27 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0028 | Module: 4 | Type: positive | Step: perform operation #28 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0029 | Module: 5 | Type: negative | Step: perform operation #29 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0030 | Module: 1 | Type: positive | Step: perform operation #30 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0031 | Module: 2 | Type: negative | Step: perform operation #31 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0032 | Module: 3 | Type: positive | Step: perform operation #32 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0033 | Module: 4 | Type: negative | Step: perform operation #33 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0034 | Module: 5 | Type: positive | Step: perform operation #34 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0035 | Module: 1 | Type: negative | Step: perform operation #35 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0036 | Module: 2 | Type: positive | Step: perform operation #36 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0037 | Module: 3 | Type: negative | Step: perform operation #37 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0038 | Module: 4 | Type: positive | Step: perform operation #38 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0039 | Module: 5 | Type: negative | Step: perform operation #39 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0040 | Module: 1 | Type: positive | Step: perform operation #40 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0041 | Module: 2 | Type: negative | Step: perform operation #41 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0042 | Module: 3 | Type: positive | Step: perform operation #42 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0043 | Module: 4 | Type: negative | Step: perform operation #43 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0044 | Module: 5 | Type: positive | Step: perform operation #44 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0045 | Module: 1 | Type: negative | Step: perform operation #45 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0046 | Module: 2 | Type: positive | Step: perform operation #46 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0047 | Module: 3 | Type: negative | Step: perform operation #47 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0048 | Module: 4 | Type: positive | Step: perform operation #48 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0049 | Module: 5 | Type: negative | Step: perform operation #49 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0050 | Module: 1 | Type: positive | Step: perform operation #50 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0051 | Module: 2 | Type: negative | Step: perform operation #51 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0052 | Module: 3 | Type: positive | Step: perform operation #52 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0053 | Module: 4 | Type: negative | Step: perform operation #53 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0054 | Module: 5 | Type: positive | Step: perform operation #54 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0055 | Module: 1 | Type: negative | Step: perform operation #55 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0056 | Module: 2 | Type: positive | Step: perform operation #56 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0057 | Module: 3 | Type: negative | Step: perform operation #57 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0058 | Module: 4 | Type: positive | Step: perform operation #58 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0059 | Module: 5 | Type: negative | Step: perform operation #59 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0060 | Module: 1 | Type: positive | Step: perform operation #60 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0061 | Module: 2 | Type: negative | Step: perform operation #61 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0062 | Module: 3 | Type: positive | Step: perform operation #62 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0063 | Module: 4 | Type: negative | Step: perform operation #63 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0064 | Module: 5 | Type: positive | Step: perform operation #64 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0065 | Module: 1 | Type: negative | Step: perform operation #65 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0066 | Module: 2 | Type: positive | Step: perform operation #66 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0067 | Module: 3 | Type: negative | Step: perform operation #67 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0068 | Module: 4 | Type: positive | Step: perform operation #68 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0069 | Module: 5 | Type: negative | Step: perform operation #69 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0070 | Module: 1 | Type: positive | Step: perform operation #70 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0071 | Module: 2 | Type: negative | Step: perform operation #71 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0072 | Module: 3 | Type: positive | Step: perform operation #72 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0073 | Module: 4 | Type: negative | Step: perform operation #73 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0074 | Module: 5 | Type: positive | Step: perform operation #74 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0075 | Module: 1 | Type: negative | Step: perform operation #75 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0076 | Module: 2 | Type: positive | Step: perform operation #76 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0077 | Module: 3 | Type: negative | Step: perform operation #77 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0078 | Module: 4 | Type: positive | Step: perform operation #78 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0079 | Module: 5 | Type: negative | Step: perform operation #79 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0080 | Module: 1 | Type: positive | Step: perform operation #80 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0081 | Module: 2 | Type: negative | Step: perform operation #81 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0082 | Module: 3 | Type: positive | Step: perform operation #82 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0083 | Module: 4 | Type: negative | Step: perform operation #83 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0084 | Module: 5 | Type: positive | Step: perform operation #84 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0085 | Module: 1 | Type: negative | Step: perform operation #85 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0086 | Module: 2 | Type: positive | Step: perform operation #86 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0087 | Module: 3 | Type: negative | Step: perform operation #87 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0088 | Module: 4 | Type: positive | Step: perform operation #88 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0089 | Module: 5 | Type: negative | Step: perform operation #89 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0090 | Module: 1 | Type: positive | Step: perform operation #90 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0091 | Module: 2 | Type: negative | Step: perform operation #91 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0092 | Module: 3 | Type: positive | Step: perform operation #92 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0093 | Module: 4 | Type: negative | Step: perform operation #93 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0094 | Module: 5 | Type: positive | Step: perform operation #94 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0095 | Module: 1 | Type: negative | Step: perform operation #95 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0096 | Module: 2 | Type: positive | Step: perform operation #96 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0097 | Module: 3 | Type: negative | Step: perform operation #97 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0098 | Module: 4 | Type: positive | Step: perform operation #98 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0099 | Module: 5 | Type: negative | Step: perform operation #99 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0100 | Module: 1 | Type: positive | Step: perform operation #100 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0101 | Module: 2 | Type: negative | Step: perform operation #101 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0102 | Module: 3 | Type: positive | Step: perform operation #102 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0103 | Module: 4 | Type: negative | Step: perform operation #103 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0104 | Module: 5 | Type: positive | Step: perform operation #104 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0105 | Module: 1 | Type: negative | Step: perform operation #105 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0106 | Module: 2 | Type: positive | Step: perform operation #106 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0107 | Module: 3 | Type: negative | Step: perform operation #107 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0108 | Module: 4 | Type: positive | Step: perform operation #108 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0109 | Module: 5 | Type: negative | Step: perform operation #109 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0110 | Module: 1 | Type: positive | Step: perform operation #110 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0111 | Module: 2 | Type: negative | Step: perform operation #111 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0112 | Module: 3 | Type: positive | Step: perform operation #112 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0113 | Module: 4 | Type: negative | Step: perform operation #113 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0114 | Module: 5 | Type: positive | Step: perform operation #114 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0115 | Module: 1 | Type: negative | Step: perform operation #115 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0116 | Module: 2 | Type: positive | Step: perform operation #116 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0117 | Module: 3 | Type: negative | Step: perform operation #117 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0118 | Module: 4 | Type: positive | Step: perform operation #118 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0119 | Module: 5 | Type: negative | Step: perform operation #119 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0120 | Module: 1 | Type: positive | Step: perform operation #120 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0121 | Module: 2 | Type: negative | Step: perform operation #121 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0122 | Module: 3 | Type: positive | Step: perform operation #122 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0123 | Module: 4 | Type: negative | Step: perform operation #123 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0124 | Module: 5 | Type: positive | Step: perform operation #124 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0125 | Module: 1 | Type: negative | Step: perform operation #125 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0126 | Module: 2 | Type: positive | Step: perform operation #126 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0127 | Module: 3 | Type: negative | Step: perform operation #127 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0128 | Module: 4 | Type: positive | Step: perform operation #128 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0129 | Module: 5 | Type: negative | Step: perform operation #129 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0130 | Module: 1 | Type: positive | Step: perform operation #130 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0131 | Module: 2 | Type: negative | Step: perform operation #131 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0132 | Module: 3 | Type: positive | Step: perform operation #132 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0133 | Module: 4 | Type: negative | Step: perform operation #133 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0134 | Module: 5 | Type: positive | Step: perform operation #134 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0135 | Module: 1 | Type: negative | Step: perform operation #135 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0136 | Module: 2 | Type: positive | Step: perform operation #136 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0137 | Module: 3 | Type: negative | Step: perform operation #137 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0138 | Module: 4 | Type: positive | Step: perform operation #138 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0139 | Module: 5 | Type: negative | Step: perform operation #139 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0140 | Module: 1 | Type: positive | Step: perform operation #140 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0141 | Module: 2 | Type: negative | Step: perform operation #141 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0142 | Module: 3 | Type: positive | Step: perform operation #142 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0143 | Module: 4 | Type: negative | Step: perform operation #143 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0144 | Module: 5 | Type: positive | Step: perform operation #144 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0145 | Module: 1 | Type: negative | Step: perform operation #145 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0146 | Module: 2 | Type: positive | Step: perform operation #146 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0147 | Module: 3 | Type: negative | Step: perform operation #147 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0148 | Module: 4 | Type: positive | Step: perform operation #148 | Expected: system responds predictably | Actual: _______ | Pass: Y/N
TC-0149 | Module: 5 | Type: negative | Step: perform operation #149 | Expected: system responds predictably | Actual: _______ | Pass: Y/N

19A. APPENDIX — MERMAID ER (SOURCE)
===================================

erDiagram
    ADMIN { long id PK; string name; string email UK; string password }
    DEPARTMENT { long id PK; string name UK; string description }
    PATIENT { long patient_id PK; string name; string email UK; string password; string phone }
    DOCTOR { long doctor_id PK; string name; string email UK; string department_id FK;
             string specialization; string availability }
    APPOINTMENT { long appointment_id PK; long patient_id FK; long doctor_id FK;
                 date date; time time; string status }
    TIME_SLOT { long id PK; long doctor_id FK; date slot_date; time start_time; time end_time;
               boolean available }
    DEPARTMENT ||--o{ DOCTOR : has
    PATIENT ||--o{ APPOINTMENT : books
    DOCTOR ||--o{ APPOINTMENT : has
    DOCTOR ||--o{ TIME_SLOT : has

19B. APPENDIX — MERMAID DFD LEVEL 0 & 1 (SOURCE)
================================================

flowchart LR
    Patient([Patient]) <--> SYS[Doctor Appointment Booking System]
    Doctor([Doctor]) <--> SYS
    Admin([Admin]) <--> SYS

(Level 1 detailed block — see DFD-DIAGRAM.html in static folder or ER-DFD-PROJECT-CONTENT.txt)
19C. REVISION LOG (FILL MANUALLY)
=================================

Rev 01 | Date ______ | Author ______ | Summary of changes ____________________________
Rev 02 | Date ______ | Author ______ | Summary of changes ____________________________
Rev 03 | Date ______ | Author ______ | Summary of changes ____________________________
Rev 04 | Date ______ | Author ______ | Summary of changes ____________________________
Rev 05 | Date ______ | Author ______ | Summary of changes ____________________________
Rev 06 | Date ______ | Author ______ | Summary of changes ____________________________
Rev 07 | Date ______ | Author ______ | Summary of changes ____________________________
Rev 08 | Date ______ | Author ______ | Summary of changes ____________________________
Rev 09 | Date ______ | Author ______ | Summary of changes ____________________________
Rev 10 | Date ______ | Author ______ | Summary of changes ____________________________
Rev 11 | Date ______ | Author ______ | Summary of changes ____________________________
Rev 12 | Date ______ | Author ______ | Summary of changes ____________________________
Rev 13 | Date ______ | Author ______ | Summary of changes ____________________________
Rev 14 | Date ______ | Author ______ | Summary of changes ____________________________
Rev 15 | Date ______ | Author ______ | Summary of changes ____________________________
Rev 16 | Date ______ | Author ______ | Summary of changes ____________________________
Rev 17 | Date ______ | Author ______ | Summary of changes ____________________________
Rev 18 | Date ______ | Author ______ | Summary of changes ____________________________
Rev 19 | Date ______ | Author ______ | Summary of changes ____________________________
Rev 20 | Date ______ | Author ______ | Summary of changes ____________________________
Rev 21 | Date ______ | Author ______ | Summary of changes ____________________________
Rev 22 | Date ______ | Author ______ | Summary of changes ____________________________
Rev 23 | Date ______ | Author ______ | Summary of changes ____________________________
Rev 24 | Date ______ | Author ______ | Summary of changes ____________________________
Rev 25 | Date ______ | Author ______ | Summary of changes ____________________________
Rev 26 | Date ______ | Author ______ | Summary of changes ____________________________
Rev 27 | Date ______ | Author ______ | Summary of changes ____________________________
Rev 28 | Date ______ | Author ______ | Summary of changes ____________________________
Rev 29 | Date ______ | Author ______ | Summary of changes ____________________________
Rev 30 | Date ______ | Author ______ | Summary of changes ____________________________
Rev 31 | Date ______ | Author ______ | Summary of changes ____________________________
Rev 32 | Date ______ | Author ______ | Summary of changes ____________________________
Rev 33 | Date ______ | Author ______ | Summary of changes ____________________________
Rev 34 | Date ______ | Author ______ | Summary of changes ____________________________
Rev 35 | Date ______ | Author ______ | Summary of changes ____________________________
Rev 36 | Date ______ | Author ______ | Summary of changes ____________________________
Rev 37 | Date ______ | Author ______ | Summary of changes ____________________________
Rev 38 | Date ______ | Author ______ | Summary of changes ____________________________
Rev 39 | Date ______ | Author ______ | Summary of changes ____________________________
Rev 40 | Date ______ | Author ______ | Summary of changes ____________________________
Rev 41 | Date ______ | Author ______ | Summary of changes ____________________________
Rev 42 | Date ______ | Author ______ | Summary of changes ____________________________
Rev 43 | Date ______ | Author ______ | Summary of changes ____________________________
Rev 44 | Date ______ | Author ______ | Summary of changes ____________________________
20. SOURCE CODE WITH DESCRIPTIONS (COLLEGE PROJECT FORMAT)
==========================================================

This chapter follows a typical B.Tech / MCA project report layout: for each important file
you get (1) file path, (2) a plain-English description suitable for ‘Aim’, ‘Working’, and
viva answers, (3) the full source listing copied from the repository. Figures and
screenshots should be added in Word from your own run of the application. Regenerate this
document anytime with: python3 scripts/generate_long_report.py

MODULE — HospitalManagementApplication.java
-------------------------------------------

File path: src/main/java/com/hospital/HospitalManagementApplication.java

Description (for project report / viva preparation)
--------------------------------------------------
This is the main entry class of the Spring Boot application. The @SpringBootApplication
annotation enables auto-configuration, component scanning, and configuration class
detection.

The main() method calls SpringApplication.run(), which starts the embedded web server
(Tomcat by default) and loads all beans (controllers, services, repositories, security
filter). For the college project report, this file proves that the backend is a standalone
executable JAR and not a separate application server install.

Source code
--------------------------------------------------
package com.hospital;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class HospitalManagementApplication {

    public static void main(String[] args) {
        SpringApplication.run(HospitalManagementApplication.class, args);
    }
}

MODULE — AuthController.java
----------------------------

File path: src/main/java/com/hospital/controller/AuthController.java

Description (for project report / viva preparation)
--------------------------------------------------
This class implements the Authentication REST API under the base path /api/auth.

It exposes three endpoints: POST /login (validates credentials and returns token + role),
POST /register (creates a new patient account via PatientService), and POST /logout
(invalidates the token from the X-Auth-Token header).

The controller follows the typical Spring pattern: thin layer — validation with @Valid on
request bodies, delegation to AuthService / PatientService, and ResponseEntity for HTTP
status and JSON body. This module connects the HTML/JavaScript frontend login forms to the
server.

Source code
--------------------------------------------------
package com.hospital.controller;

import com.hospital.dto.LoginRequest;
import com.hospital.dto.LoginResponse;
import com.hospital.entity.Patient;
import com.hospital.service.AuthService;
import com.hospital.service.PatientService;
import jakarta.servlet.http.HttpServletRequest;
import jakarta.validation.Valid;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

@RestController
@RequestMapping("/api/auth")
public class AuthController {

    private final AuthService authService;
    private final PatientService patientService;

    public AuthController(AuthService authService, PatientService patientService) {
        this.authService = authService;
        this.patientService = patientService;
    }

    @PostMapping("/login")
    public ResponseEntity<LoginResponse> login(@Valid @RequestBody LoginRequest request) {
        LoginResponse response = authService.login(request);
        return ResponseEntity.ok(response);
    }

    @PostMapping("/register")
    public ResponseEntity<?> register(@Valid @RequestBody Patient patient) {
        return ResponseEntity.ok(patientService.register(patient));
    }

    @PostMapping("/logout")
    public ResponseEntity<Void> logout(HttpServletRequest request) {
        String token = request.getHeader("X-Auth-Token");
        authService.logout(token);
        return ResponseEntity.ok().build();
    }
}

MODULE — AuthService.java
-------------------------

File path: src/main/java/com/hospital/service/AuthService.java

Description (for project report / viva preparation)
--------------------------------------------------
AuthService contains the business logic for login. It reads the requested role (ADMIN,
DOCTOR, PATIENT), looks up the user in the corresponding JPA repository by email, and
compares the password.

On success it creates a random token through TokenStore and returns LoginResponse with
success flag, role, user ID, name, and token. On failure it returns a response with success
false and an error message.

Note for viva: in this demo project passwords are compared as plain text for simplicity; in
a production system you must use password hashing (e.g. BCrypt) and never store plain
passwords.

Source code
--------------------------------------------------
package com.hospital.service;

import com.hospital.dto.LoginRequest;
import com.hospital.dto.LoginResponse;
import com.hospital.entity.Admin;
import com.hospital.entity.Doctor;
import com.hospital.entity.Patient;
import com.hospital.repository.AdminRepository;
import com.hospital.repository.DoctorRepository;
import com.hospital.repository.PatientRepository;
import org.springframework.stereotype.Service;

import java.util.UUID;

@Service
public class AuthService {

    private final AdminRepository adminRepository;
    private final DoctorRepository doctorRepository;
    private final PatientRepository patientRepository;
    private final TokenStore tokenStore;

    public AuthService(AdminRepository adminRepository, DoctorRepository doctorRepository,
                        PatientRepository patientRepository, TokenStore tokenStore) {
        this.adminRepository = adminRepository;
        this.doctorRepository = doctorRepository;
        this.patientRepository = patientRepository;
        this.tokenStore = tokenStore;
    }

    public LoginResponse login(LoginRequest request) {
        String role = request.getRole().toUpperCase();
        switch (role) {
            case "ADMIN" -> {
                return adminRepository.findByEmail(request.getEmail())
                        .filter(a -> a.getPassword().equals(request.getPassword()))
                        .map(this::buildAdminResponse)
                        .orElse(LoginResponse.builder().success(false).message("Invalid credentials").build());
            }
            case "DOCTOR" -> {
                return doctorRepository.findByEmail(request.getEmail())
                        .filter(d -> d.getPassword().equals(request.getPassword()))
                        .map(this::buildDoctorResponse)
                        .orElse(LoginResponse.builder().success(false).message("Invalid credentials").build());
            }
            case "PATIENT" -> {
                return patientRepository.findByEmail(request.getEmail())
                        .filter(p -> p.getPassword().equals(request.getPassword()))
                        .map(this::buildPatientResponse)
                        .orElse(LoginResponse.builder().success(false).message("Invalid credentials").build());
            }
            default -> {
                return LoginResponse.builder().success(false).message("Invalid role").build();
            }
        }
    }

    private LoginResponse buildAdminResponse(Admin a) {
        String token = tokenStore.createToken("ADMIN", a.getId());
        return LoginResponse.builder()
                .success(true)
                .message("Login successful")
                .role("ADMIN")
                .userId(a.getId())
                .name(a.getName())
                .token(token)
                .build();
    }

    private LoginResponse buildDoctorResponse(Doctor d) {
        String token = tokenStore.createToken("DOCTOR", d.getDoctorId());
        return LoginResponse.builder()
                .success(true)
                .message("Login successful")
                .role("DOCTOR")
                .userId(d.getDoctorId())
                .name(d.getName())
                .token(token)
                .build();
    }

    private LoginResponse buildPatientResponse(Patient p) {
        String token = tokenStore.createToken("PATIENT", p.getPatientId());
        return LoginResponse.builder()
                .success(true)
                .message("Login successful")
                .role("PATIENT")
                .userId(p.getPatientId())
                .name(p.getName())
                .token(token)
                .build();
    }

    public void logout(String token) {
        tokenStore.removeToken(token);
    }
}

MODULE — SecurityConfig.java
----------------------------

File path: src/main/java/com/hospital/config/SecurityConfig.java

Description (for project report / viva preparation)
--------------------------------------------------
SecurityConfig defines Spring Security’s filter chain. CSRF is disabled because the REST API
is token-based; session creation is STATELESS.

All HTTP requests are permitted at the authorization rule level because fine-grained
protection is implemented in the custom AuthFilter (path and role checks). AuthFilter is
registered before UsernamePasswordAuthenticationFilter.

This shows separation between Spring Security framework setup and application-specific token
rules.

Source code
--------------------------------------------------
package com.hospital.config;

import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.security.config.annotation.web.builders.HttpSecurity;
import org.springframework.security.config.annotation.web.configuration.EnableWebSecurity;
import org.springframework.security.config.http.SessionCreationPolicy;
import org.springframework.security.web.SecurityFilterChain;
import org.springframework.security.web.authentication.UsernamePasswordAuthenticationFilter;

@Configuration
@EnableWebSecurity
public class SecurityConfig {

    private final AuthFilter authFilter;

    public SecurityConfig(AuthFilter authFilter) {
        this.authFilter = authFilter;
    }

    @Bean
    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
        http
                .csrf(csrf -> csrf.disable())
                .sessionManagement(s -> s.sessionCreationPolicy(SessionCreationPolicy.STATELESS))
                .authorizeHttpRequests(auth -> auth.anyRequest().permitAll())
                .addFilterBefore(authFilter, UsernamePasswordAuthenticationFilter.class);
        return http.build();
    }
}

MODULE — AuthFilter.java
------------------------

File path: src/main/java/com/hospital/config/AuthFilter.java

Description (for project report / viva preparation)
--------------------------------------------------
AuthFilter is a OncePerRequestFilter that runs on every request. Paths such as login,
register, static pages, and some public APIs are skipped (white-list).

For other /api/ calls it requires the X-Auth-Token header, validates the token via
TokenStore, and enforces role-based access: /api/admin/* for ADMIN, /api/doctor/* for
DOCTOR, /api/patient/* for PATIENT. On success it stores userId and userRole as request
attributes for controllers.

This file is central to explain “how unauthorized users are blocked” in the college project.

Source code
--------------------------------------------------
package com.hospital.config;

import com.hospital.service.TokenStore;
import jakarta.servlet.FilterChain;
import jakarta.servlet.ServletException;
import jakarta.servlet.http.HttpServletRequest;
import jakarta.servlet.http.HttpServletResponse;
import org.springframework.stereotype.Component;
import org.springframework.web.filter.OncePerRequestFilter;

import java.io.IOException;
import java.util.List;

@Component
public class AuthFilter extends OncePerRequestFilter {

    private final TokenStore tokenStore;

    public AuthFilter(TokenStore tokenStore) {
        this.tokenStore = tokenStore;
    }

    private static final String ROOT_PATH = "/";

    private static final List<String> PUBLIC_PATH_PREFIXES = List.of(
            "/api/auth/login",
            "/api/auth/register",
            "/api/departments/list",
            "/api/doctors/by-department",
            "/index.html",
            "/css/",
            "/js/",
            "/pages/"
    );

    @Override
    protected void doFilterInternal(HttpServletRequest request, HttpServletResponse response, FilterChain filterChain)
            throws ServletException, IOException {
        String path = request.getRequestURI();
        if (isPublicPath(path)) {
            filterChain.doFilter(request, response);
            return;
        }

        if (!path.startsWith("/api/")) {
            filterChain.doFilter(request, response);
            return;
        }

        String token = request.getHeader("X-Auth-Token");
        if (token == null || token.isBlank()) {
            response.setStatus(HttpServletResponse.SC_UNAUTHORIZED);
            response.setContentType("application/json");
            response.getWriter().write("{\"error\":\"Missing or invalid token\"}");
            return;
        }

        TokenStore.TokenInfo info = tokenStore.getTokenInfo(token);
        if (info == null) {
            response.setStatus(HttpServletResponse.SC_UNAUTHORIZED);
            response.setContentType("application/json");
            response.getWriter().write("{\"error\":\"Invalid or expired token\"}");
            return;
        }

        if (path.startsWith("/api/admin/") && !"ADMIN".equals(info.role())) {
            response.setStatus(HttpServletResponse.SC_FORBIDDEN);
            response.setContentType("application/json");
            response.getWriter().write("{\"error\":\"Admin access required\"}");
            return;
        }
        if (path.startsWith("/api/doctor/") && !"DOCTOR".equals(info.role())) {
            response.setStatus(HttpServletResponse.SC_FORBIDDEN);
            response.setContentType("application/json");
            response.getWriter().write("{\"error\":\"Doctor access required\"}");
            return;
        }
        if (path.startsWith("/api/patient/") && !"PATIENT".equals(info.role())) {
            response.setStatus(HttpServletResponse.SC_FORBIDDEN);
            response.setContentType("application/json");
            response.getWriter().write("{\"error\":\"Patient access required\"}");
            return;
        }

        request.setAttribute("userId", info.userId());
        request.setAttribute("userRole", info.role());
        filterChain.doFilter(request, response);
    }

    private boolean isPublicPath(String path) {
        if (ROOT_PATH.equals(path)) {
            return true;
        }
        return PUBLIC_PATH_PREFIXES.stream().anyMatch(path::startsWith);
    }
}

MODULE — PatientController.java
-------------------------------

File path: src/main/java/com/hospital/controller/PatientController.java

Description (for project report / viva preparation)
--------------------------------------------------
PatientController serves all patient-facing REST endpoints under /api/patient. It uses the
authenticated user ID from the request attribute set by AuthFilter.

Typical operations: list departments, list doctors by department, book an appointment
(doctor ID, date, time query parameters), list and cancel own appointments, get and update
profile.

This controller demonstrates REST design with GET/POST/DELETE/PUT and use of LocalDate /
LocalTime formatting for booking.

Source code
--------------------------------------------------
package com.hospital.controller;

import com.hospital.dto.AppointmentDTO;
import com.hospital.dto.DepartmentDTO;
import com.hospital.dto.DoctorDTO;
import com.hospital.dto.PatientDTO;
import com.hospital.entity.Patient;
import com.hospital.service.AppointmentService;
import com.hospital.service.DepartmentService;
import com.hospital.service.DoctorService;
import com.hospital.service.PatientService;
import jakarta.servlet.http.HttpServletRequest;
import org.springframework.format.annotation.DateTimeFormat;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

import java.time.LocalDate;
import java.time.LocalTime;
import java.util.List;

@RestController
@RequestMapping("/api/patient")
public class PatientController {

    private final DepartmentService departmentService;
    private final DoctorService doctorService;
    private final AppointmentService appointmentService;
    private final PatientService patientService;

    public PatientController(DepartmentService departmentService, DoctorService doctorService,
                             AppointmentService appointmentService, PatientService patientService) {
        this.departmentService = departmentService;
        this.doctorService = doctorService;
        this.appointmentService = appointmentService;
        this.patientService = patientService;
    }

    private Long getPatientId(HttpServletRequest request) {
        return (Long) request.getAttribute("userId");
    }

    @GetMapping("/departments")
    public ResponseEntity<List<DepartmentDTO>> getDepartments() {
        return ResponseEntity.ok(departmentService.findAll());
    }

    @GetMapping("/doctors/by-department/{departmentId}")
    public ResponseEntity<List<DoctorDTO>> getDoctorsByDepartment(@PathVariable Long departmentId) {
        return ResponseEntity.ok(doctorService.findByDepartment(departmentId));
    }

    @PostMapping("/appointments")
    public ResponseEntity<AppointmentDTO> bookAppointment(
            HttpServletRequest request,
            @RequestParam Long doctorId,
            @RequestParam @DateTimeFormat(iso = DateTimeFormat.ISO.DATE) LocalDate date,
            @RequestParam @DateTimeFormat(iso = DateTimeFormat.ISO.TIME) LocalTime time) {
        Long patientId = getPatientId(request);
        return ResponseEntity.ok(appointmentService.book(patientId, doctorId, date, time));
    }

    @GetMapping("/appointments")
    public ResponseEntity<List<AppointmentDTO>> getMyAppointments(HttpServletRequest request) {
        Long patientId = getPatientId(request);
        return ResponseEntity.ok(appointmentService.findByPatient(patientId));
    }

    @DeleteMapping("/appointments/{id}")
    public ResponseEntity<Void> cancelAppointment(@PathVariable Long id, HttpServletRequest request) {
        Long patientId = getPatientId(request);
        appointmentService.cancel(id, patientId);
        return ResponseEntity.ok().build();
    }

    @GetMapping("/profile")
    public ResponseEntity<PatientDTO> getProfile(HttpServletRequest request) {
        Long patientId = getPatientId(request);
        return ResponseEntity.ok(patientService.findById(patientId));
    }

    @PutMapping("/profile")
    public ResponseEntity<PatientDTO> updateProfile(HttpServletRequest request, @RequestBody Patient updates) {
        Long patientId = getPatientId(request);
        return ResponseEntity.ok(patientService.update(patientId, updates));
    }
}

MODULE — AdminController.java
-----------------------------

File path: src/main/java/com/hospital/controller/AdminController.java

Description (for project report / viva preparation)
--------------------------------------------------
AdminController groups administrator APIs under /api/admin. Only users with ADMIN role can
pass AuthFilter for these paths.

Functionality includes CRUD for departments and doctors, listing patients and appointments,
and related management operations as implemented in the service layer. This module documents
how hospital staff maintain master data and monitor all appointments in one place.

Source code
--------------------------------------------------
package com.hospital.controller;

import com.hospital.dto.*;
import com.hospital.entity.Doctor;
import com.hospital.entity.Patient;
import com.hospital.service.*;
import jakarta.servlet.http.HttpServletRequest;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

import java.util.List;

@RestController
@RequestMapping("/api/admin")
public class AdminController {

    private final DepartmentService departmentService;
    private final DoctorService doctorService;
    private final PatientService patientService;
    private final AppointmentService appointmentService;

    public AdminController(DepartmentService departmentService, DoctorService doctorService,
                           PatientService patientService, AppointmentService appointmentService) {
        this.departmentService = departmentService;
        this.doctorService = doctorService;
        this.patientService = patientService;
        this.appointmentService = appointmentService;
    }

    private Long getAdminId(HttpServletRequest request) {
        return (Long) request.getAttribute("userId");
    }

    // --- Departments ---
    @GetMapping("/departments")
    public ResponseEntity<List<DepartmentDTO>> getAllDepartments() {
        return ResponseEntity.ok(departmentService.findAll());
    }

    @PostMapping("/departments")
    public ResponseEntity<DepartmentDTO> createDepartment(@RequestBody DepartmentDTO dto) {
        return ResponseEntity.ok(departmentService.create(dto));
    }

    @PutMapping("/departments/{id}")
    public ResponseEntity<DepartmentDTO> updateDepartment(@PathVariable Long id, @RequestBody DepartmentDTO dto) {
        return ResponseEntity.ok(departmentService.update(id, dto));
    }

    @DeleteMapping("/departments/{id}")
    public ResponseEntity<Void> deleteDepartment(@PathVariable Long id) {
        departmentService.delete(id);
        return ResponseEntity.ok().build();
    }

    // --- Doctors ---
    @GetMapping("/doctors")
    public ResponseEntity<List<DoctorDTO>> getAllDoctors() {
        return ResponseEntity.ok(doctorService.findAll());
    }

    @PostMapping("/doctors")
    public ResponseEntity<DoctorDTO> addDoctor(@RequestBody com.hospital.dto.DoctorCreateRequest req) {
        Doctor d = new Doctor();
        d.setName(req.getName());
        d.setEmail(req.getEmail());
        d.setPassword(req.getPassword());
        d.setSpecialization(req.getSpecialization());
        d.setAvailability(req.getAvailability());
        return ResponseEntity.ok(doctorService.create(d, req.getDepartmentId()));
    }

    @PutMapping("/doctors/{id}")
    public ResponseEntity<DoctorDTO> updateDoctor(@PathVariable Long id, @RequestBody com.hospital.dto.DoctorUpdateRequest req) {
        Doctor d = new Doctor();
        d.setName(req.getName());
        d.setEmail(req.getEmail());
        d.setPassword(req.getPassword());
        d.setSpecialization(req.getSpecialization());
        d.setAvailability(req.getAvailability());
        return ResponseEntity.ok(doctorService.update(id, d, req.getDepartmentId()));
    }

    @DeleteMapping("/doctors/{id}")
    public ResponseEntity<Void> deleteDoctor(@PathVariable Long id) {
        doctorService.delete(id);
        return ResponseEntity.ok().build();
    }

    // --- Patients (manage users) ---
    @GetMapping("/patients")
    public ResponseEntity<List<PatientDTO>> getAllPatients() {
        return ResponseEntity.ok(patientService.findAll());
    }

    @PutMapping("/patients/{id}")
    public ResponseEntity<PatientDTO> updatePatient(@PathVariable Long id, @RequestBody Patient updates) {
        return ResponseEntity.ok(patientService.update(id, updates));
    }

    @DeleteMapping("/patients/{id}")
    public ResponseEntity<Void> deletePatient(@PathVariable Long id) {
        patientService.delete(id);
        return ResponseEntity.ok().build();
    }

    // --- Appointments ---
    @GetMapping("/appointments")
    public ResponseEntity<List<AppointmentDTO>> getAllAppointments() {
        return ResponseEntity.ok(appointmentService.findAll());
    }

}

MODULE — DoctorController.java
------------------------------

File path: src/main/java/com/hospital/controller/DoctorController.java

Description (for project report / viva preparation)
--------------------------------------------------
DoctorController exposes /api/doctor endpoints for the doctor dashboard: listing
appointments assigned to the logged-in doctor, updating appointment status (e.g. APPROVED,
COMPLETED, CANCELLED), viewing patient details for an appointment, and managing time slots.

It ties the doctor workflow (queue + schedule) to AppointmentService, PatientService, and
TimeSlotService.

Source code
--------------------------------------------------
package com.hospital.controller;

import com.hospital.dto.AppointmentDTO;
import com.hospital.dto.TimeSlotDTO;
import com.hospital.entity.Appointment.AppointmentStatus;
import com.hospital.service.AppointmentService;
import com.hospital.service.PatientService;
import com.hospital.service.TimeSlotService;
import jakarta.servlet.http.HttpServletRequest;
import org.springframework.format.annotation.DateTimeFormat;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

import java.time.LocalDate;
import java.time.LocalTime;
import java.util.List;

@RestController
@RequestMapping("/api/doctor")
public class DoctorController {

    private final AppointmentService appointmentService;
    private final PatientService patientService;
    private final TimeSlotService timeSlotService;

    public DoctorController(AppointmentService appointmentService, PatientService patientService,
                            TimeSlotService timeSlotService) {
        this.appointmentService = appointmentService;
        this.patientService = patientService;
        this.timeSlotService = timeSlotService;
    }

    private Long getDoctorId(HttpServletRequest request) {
        return (Long) request.getAttribute("userId");
    }

    @GetMapping("/appointments")
    public ResponseEntity<List<AppointmentDTO>> getMyAppointments(HttpServletRequest request) {
        Long doctorId = getDoctorId(request);
        return ResponseEntity.ok(appointmentService.findByDoctor(doctorId));
    }

    @PutMapping("/appointments/{id}/status")
    public ResponseEntity<AppointmentDTO> updateAppointmentStatus(
            @PathVariable Long id,
            @RequestParam String status,
            HttpServletRequest request) {
        Long doctorId = getDoctorId(request);
        AppointmentDTO app = appointmentService.findById(id);
        if (!app.getDoctorId().equals(doctorId)) {
            return ResponseEntity.status(403).build();
        }
        AppointmentStatus s = AppointmentStatus.valueOf(status.toUpperCase());
        if (s != AppointmentStatus.APPROVED && s != AppointmentStatus.COMPLETED && s != AppointmentStatus.CANCELLED) {
            return ResponseEntity.badRequest().build();
        }
        return ResponseEntity.ok(appointmentService.updateStatus(id, s));
    }

    @GetMapping("/appointments/{id}/patient")
    public ResponseEntity<?> getPatientDetails(@PathVariable Long id, HttpServletRequest request) {
        Long doctorId = getDoctorId(request);
        AppointmentDTO app = appointmentService.findById(id);
        if (!app.getDoctorId().equals(doctorId)) {
            return ResponseEntity.status(403).build();
        }
        return ResponseEntity.ok(patientService.findById(app.getPatientId()));
    }

    @GetMapping("/time-slots")
    public ResponseEntity<List<TimeSlotDTO>> getMyTimeSlots(HttpServletRequest request) {
        Long doctorId = getDoctorId(request);
        return ResponseEntity.ok(timeSlotService.findByDoctor(doctorId));
    }

    @PostMapping("/time-slots")
    public ResponseEntity<TimeSlotDTO> addTimeSlot(
            HttpServletRequest request,
            @RequestParam @DateTimeFormat(iso = DateTimeFormat.ISO.DATE) LocalDate slotDate,
            @RequestParam @DateTimeFormat(iso = DateTimeFormat.ISO.TIME) LocalTime startTime,
            @RequestParam @DateTimeFormat(iso = DateTimeFormat.ISO.TIME) LocalTime endTime) {
        Long doctorId = getDoctorId(request);
        return ResponseEntity.ok(timeSlotService.create(doctorId, slotDate, startTime, endTime));
    }

    @DeleteMapping("/time-slots/{id}")
    public ResponseEntity<Void> deleteTimeSlot(@PathVariable Long id, HttpServletRequest request) {
        Long doctorId = getDoctorId(request);
        TimeSlotDTO slot = timeSlotService.findById(id);
        if (!slot.getDoctorId().equals(doctorId)) {
            return ResponseEntity.status(403).build();
        }
        timeSlotService.delete(id);
        return ResponseEntity.ok().build();
    }
}

MODULE — Patient.java
---------------------

File path: src/main/java/com/hospital/entity/Patient.java

Description (for project report / viva preparation)
--------------------------------------------------
Patient is a JPA entity mapped to table patients. Primary key patient_id is generated by
identity. Fields include name, unique email, password, optional phone.

The @OneToMany relationship with Appointment (mappedBy patient, cascade ALL, orphanRemoval)
means deleting a patient can remove dependent appointments according to cascade rules —
important to mention when discussing database referential integrity in the report.

Source code
--------------------------------------------------
package com.hospital.entity;

import jakarta.persistence.*;

import java.util.ArrayList;
import java.util.List;

@Entity
@Table(name = "patients")
public class Patient {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    @Column(name = "patient_id")
    private Long patientId;

    @Column(nullable = false)
    private String name;

    @Column(nullable = false, unique = true)
    private String email;

    @Column(nullable = false)
    private String password;

    private String phone;

    @OneToMany(mappedBy = "patient", cascade = CascadeType.ALL, orphanRemoval = true)
    private List<Appointment> appointments = new ArrayList<>();

    public Patient() {}
    public Long getPatientId() { return patientId; }
    public void setPatientId(Long patientId) { this.patientId = patientId; }
    public String getName() { return name; }
    public void setName(String name) { this.name = name; }
    public String getEmail() { return email; }
    public void setEmail(String email) { this.email = email; }
    public String getPassword() { return password; }
    public void setPassword(String password) { this.password = password; }
    public String getPhone() { return phone; }
    public void setPhone(String phone) { this.phone = phone; }
    public List<Appointment> getAppointments() { return appointments; }
    public void setAppointments(List<Appointment> appointments) { this.appointments = appointments; }
}

MODULE — Doctor.java
--------------------

File path: src/main/java/com/hospital/entity/Doctor.java

Description (for project report / viva preparation)
--------------------------------------------------
Doctor maps to table doctors. Each doctor has a @ManyToOne link to Department (required
department_id).

OneToMany relationships exist to Appointment and TimeSlot. Additional fields are
specialization and availability text. Unique email ensures one login identity per doctor
record.

This entity is the bridge between clinical identity (specialization, department) and
scheduling (slots, appointments).

Source code
--------------------------------------------------
package com.hospital.entity;

import jakarta.persistence.*;

import java.util.ArrayList;
import java.util.List;

@Entity
@Table(name = "doctors")
public class Doctor {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    @Column(name = "doctor_id")
    private Long doctorId;

    @Column(nullable = false)
    private String name;

    @Column(nullable = false, unique = true)
    private String email;

    @Column(nullable = false)
    private String password;

    @ManyToOne(fetch = FetchType.LAZY)
    @JoinColumn(name = "department_id", nullable = false)
    private Department department;

    private String specialization;

    @Column(length = 500)
    private String availability;

    @OneToMany(mappedBy = "doctor", cascade = CascadeType.ALL, orphanRemoval = true)
    private List<Appointment> appointments = new ArrayList<>();

    @OneToMany(mappedBy = "doctor", cascade = CascadeType.ALL, orphanRemoval = true)
    private List<TimeSlot> timeSlots = new ArrayList<>();

    public Doctor() {}
    public Long getDoctorId() { return doctorId; }
    public void setDoctorId(Long doctorId) { this.doctorId = doctorId; }
    public String getName() { return name; }
    public void setName(String name) { this.name = name; }
    public String getEmail() { return email; }
    public void setEmail(String email) { this.email = email; }
    public String getPassword() { return password; }
    public void setPassword(String password) { this.password = password; }
    public Department getDepartment() { return department; }
    public void setDepartment(Department department) { this.department = department; }
    public String getSpecialization() { return specialization; }
    public void setSpecialization(String specialization) { this.specialization = specialization; }
    public String getAvailability() { return availability; }
    public void setAvailability(String availability) { this.availability = availability; }
    public List<Appointment> getAppointments() { return appointments; }
    public void setAppointments(List<Appointment> appointments) { this.appointments = appointments; }
    public List<TimeSlot> getTimeSlots() { return timeSlots; }
    public void setTimeSlots(List<TimeSlot> timeSlots) { this.timeSlots = timeSlots; }
}

MODULE — Appointment.java
-------------------------

File path: src/main/java/com/hospital/entity/Appointment.java

Description (for project report / viva preparation)
--------------------------------------------------
Appointment maps to table appointments with composite logical link: many-to-one Patient and
many-to-one Doctor. Date and time use Java LocalDate and LocalTime.

status is an enum AppointmentStatus (PENDING, APPROVED, COMPLETED, CANCELLED) stored as
string in the database. Default is PENDING. This entity is the core transactional record for
the “book appointment” use case.

Source code
--------------------------------------------------
package com.hospital.entity;

import jakarta.persistence.*;

import java.time.LocalDate;
import java.time.LocalTime;

@Entity
@Table(name = "appointments")
public class Appointment {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    @Column(name = "appointment_id")
    private Long appointmentId;

    @ManyToOne(fetch = FetchType.LAZY)
    @JoinColumn(name = "patient_id", nullable = false)
    private Patient patient;

    @ManyToOne(fetch = FetchType.LAZY)
    @JoinColumn(name = "doctor_id", nullable = false)
    private Doctor doctor;

    private LocalDate date;
    private LocalTime time;

    @Enumerated(EnumType.STRING)
    @Column(nullable = false)
    private AppointmentStatus status = AppointmentStatus.PENDING;

    public enum AppointmentStatus {
        PENDING, APPROVED, COMPLETED, CANCELLED
    }

    public Appointment() {}
    public Long getAppointmentId() { return appointmentId; }
    public void setAppointmentId(Long appointmentId) { this.appointmentId = appointmentId; }
    public Patient getPatient() { return patient; }
    public void setPatient(Patient patient) { this.patient = patient; }
    public Doctor getDoctor() { return doctor; }
    public void setDoctor(Doctor doctor) { this.doctor = doctor; }
    public LocalDate getDate() { return date; }
    public void setDate(LocalDate date) { this.date = date; }
    public LocalTime getTime() { return time; }
    public void setTime(LocalTime time) { this.time = time; }
    public AppointmentStatus getStatus() { return status; }
    public void setStatus(AppointmentStatus status) { this.status = status; }
}

MODULE — Department.java
------------------------

File path: src/main/java/com/hospital/entity/Department.java

Description (for project report / viva preparation)
--------------------------------------------------
Department represents a hospital department (e.g. Cardiology). Table departments has id,
unique name, and description.

It is referenced by Doctor rows; therefore department master data must exist before
assigning doctors. The entity is simple but essential for browsing doctors by department on
the patient side.

Source code
--------------------------------------------------
package com.hospital.entity;

import jakarta.persistence.*;

@Entity
@Table(name = "departments")
public class Department {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @Column(nullable = false, unique = true)
    private String name;

    private String description;

    public Department() {}
    public Department(Long id, String name, String description) {
        this.id = id;
        this.name = name;
        this.description = description;
    }

    public Long getId() { return id; }
    public void setId(Long id) { this.id = id; }
    public String getName() { return name; }
    public void setName(String name) { this.name = name; }
    public String getDescription() { return description; }
    public void setDescription(String description) { this.description = description; }
}

MODULE — Admin.java
-------------------

File path: src/main/java/com/hospital/entity/Admin.java

Description (for project report / viva preparation)
--------------------------------------------------
Admin maps to table admins with id, name, unique email, and password. It models back-office
users who manage the system.

Separating Admin in its own table keeps role data normalized and aligns with AuthService
branch for ADMIN login.

Source code
--------------------------------------------------
package com.hospital.entity;

import jakarta.persistence.*;

@Entity
@Table(name = "admins")
public class Admin {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @Column(nullable = false)
    private String name;

    @Column(nullable = false, unique = true)
    private String email;

    @Column(nullable = false)
    private String password;

    public Admin() {}
    public Long getId() { return id; }
    public void setId(Long id) { this.id = id; }
    public String getName() { return name; }
    public void setName(String name) { this.name = name; }
    public String getEmail() { return email; }
    public void setEmail(String email) { this.email = email; }
    public String getPassword() { return password; }
    public void setPassword(String password) { this.password = password; }
}

MODULE — app.js
---------------

File path: src/main/resources/static/js/app.js

Description (for project report / viva preparation)
--------------------------------------------------
This JavaScript file is shared by HTML pages. It centralizes API access: API_BASE prefix
/api, token and role storage in sessionStorage, helpers getToken(), setAuth(), clearAuth(),
and authHeaders() which adds Content-Type and X-Auth-Token when present.

The api() function wraps fetch(), parses JSON, and throws on HTTP errors. login() posts to
/auth/login and stores returned credentials; register() posts new patient; redirectByRole()
sends users to the correct dashboard HTML; requireAuth() protects pages.

For the report, state that this layer is the “client MVC helper” — no framework like React;
plain JS keeps the college project easy to explain line-by-line.

Source code
--------------------------------------------------
const API_BASE = '/api';

function getToken() {
  return sessionStorage.getItem('token');
}

function getRole() {
  return sessionStorage.getItem('role');
}

function getUserId() {
  return sessionStorage.getItem('userId');
}

function getUserName() {
  return sessionStorage.getItem('userName');
}

function setAuth(data) {
  if (data.token) sessionStorage.setItem('token', data.token);
  if (data.role) sessionStorage.setItem('role', data.role);
  if (data.userId != null) sessionStorage.setItem('userId', String(data.userId));
  if (data.name) sessionStorage.setItem('userName', data.name);
}

function clearAuth() {
  sessionStorage.removeItem('token');
  sessionStorage.removeItem('role');
  sessionStorage.removeItem('userId');
  sessionStorage.removeItem('userName');
}

function authHeaders() {
  const token = getToken();
  return {
    'Content-Type': 'application/json',
    ...(token ? { 'X-Auth-Token': token } : {}),
  };
}

async function api(method, path, body) {
  const opts = { method, headers: authHeaders() };
  if (body) opts.body = JSON.stringify(body);
  const res = await fetch(API_BASE + path, opts);
  const text = await res.text();
  let data = null;
  try {
    data = text ? JSON.parse(text) : null;
  } catch (_) {
    data = { error: text || 'Request failed' };
  }
  if (!res.ok) {
    throw new Error(data?.error || data?.message || `HTTP ${res.status}`);
  }
  return data;
}

const apiGet = (path) => api('GET', path);
const apiPost = (path, body) => api('POST', path, body);
const apiPut = (path, body) => api('PUT', path, body);
const apiDelete = (path) => api('DELETE', path);

async function login(email, password, role) {
  const res = await apiPost('/auth/login', { email, password, role });
  if (res.success) {
    setAuth({ token: res.token, role: res.role, userId: res.userId, name: res.name });
    return res;
  }
  throw new Error(res.message || 'Login failed');
}

async function register(patient) {
  return apiPost('/auth/register', patient);
}

function redirectByRole() {
  const role = getRole();
  if (role === 'ADMIN') location.href = '/pages/admin-dashboard.html';
  else if (role === 'DOCTOR') location.href = '/pages/doctor-dashboard.html';
  else if (role === 'PATIENT') location.href = '/pages/patient-dashboard.html';
  else location.href = '/index.html';
}

function requireAuth(expectedRole) {
  if (!getToken()) {
    location.href = '/index.html';
    return false;
  }
  if (expectedRole && getRole() !== expectedRole) {
    location.href = '/index.html';
    return false;
  }
  return true;
}

function formatDate(d) {
  if (!d) return '-';
  const date = new Date(d);
  return date.toLocaleDateString();
}

function formatTime(t) {
  if (!t) return '-';
  if (typeof t === 'string' && t.includes(':')) return t.substring(0, 5);
  return t;
}
